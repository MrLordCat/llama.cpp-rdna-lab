# Candidate Hypotheses Beyond Current ngram + FlashAttention

This file tracks candidate changes that could unlock additional efficiency.

Active policy note (2026-07-20): generic performance claims now target
`Qwen3.6-27B-Q4_K_M.gguf`. The D089 safe baseline is dual ROCm
`ctx=49152,b8192/ub1024,q8_0/q8_0`, cold/no-reuse/no-warmup, with an adjacent
spec-none control. MTP n3 is the production agent profile. At 98K use the
validated one-copy scheduler; 131K remains a placement/residency stress lane.

The Q3_K_S P002/P003 policy below remains valid only for explicitly reopened
Q3 work. Its D012 2.4-TPS target math, Q3 body rejection fence, and 130K Vulkan
rows are not Q4 baseline evidence.

## Core Equation Used For Screening

Approximate decode speedup from speculative decoding:

`S_spec ~= (1 + a * (D - 1)) / (1 + o)`

Coverage-aware variant (for sparse draft coverage):

`S_spec_cov ~= (1 + (c * a_local) * (D - 1)) / (1 + o)`

Where:

- a: accepted draft token ratio
- D: drafted tokens per verification step
- o: relative overhead of draft generation + bookkeeping
- c: coverage (share of verify steps where draft was present)
- a_local: accepted/attempted inside draft-enabled steps

Approximate total wall speedup:

S_total ~= 1 / (p / S_prefill + (1 - p) / (S_spec * S_decode))

Where:

- p: baseline wall-time share spent in prefill
- S_prefill: prefill speedup (for example from attention/kernel work)
- S_decode: decode kernel speedup not covered by speculation

## Hypothesis Backlog

| ID | Idea | Why It Might Work | Expected Impact | Main Risk | First Check |
| --- | --- | --- | --- | --- | --- |
| H01 | Adaptive ngram length by local entropy | Fixed n can be too short in repetitive spans and too long in noisy spans | +5% to +20% decode | extra control overhead | acceptance rate vs entropy bucket |
| H02 | Dynamic draft length policy | Keep D small when mismatch risk is high, increase D in stable segments | +3% to +15% wall | oscillation and instability | accepted tokens per verify call |
| H03 | Hybrid router: ngram or mtp per step | Different spans favor different draft methods | +5% to +25% wall | routing overhead kills gain | net TPS with router on/off |
| H04 | Early reject bound in verify stage | Fast reject without full path when mismatch is obvious | +2% to +10% wall | quality regressions if bound is unsafe | token agreement and exact output diff |
| H05 | Flash-attn tile retarget for current RDNA path | Better tile mapping can reduce memory stalls | +5% to +20% prefill | compile/runtime complexity | prefill tok/s and occupancy |
| H06 | QKV and RoPE fusion in prefill hot path | Fewer launches and less memory traffic | +3% to +15% prefill | register pressure | kernel time breakdown |
| H07 | KV cache layout tuned for decode locality | Better cache-line behavior in long decode loops | +3% to +12% decode | migration complexity | decode tok/s and memory counters |
| H08 | Chunk-size contract alignment (model op + runtime) | Avoid pathological boundaries that trigger slow paths; current bad zone is physical n_ubatch >480 | +5% to +30% at cliff zones | model-specific behavior and boundary drift | current-best ubatch boundary sweep + physical context cap |
| H09 | Coverage-aware speculative acceptance model | Local draft acceptance overestimates global speedup if draft coverage is low | improves prediction fidelity | extra instrumentation complexity | compare implied vs effective acceptance |
| H10 | Overhead-aware speculative model by mode/config | Fixed overhead term misses severe regressions in some high-coverage MTP cases | improves prediction fidelity | more parameters and overfitting risk | backsolve implied overhead across measured cases |
| H11 | ROCm compute vbuffer chunking | A single large ROCm graph compute allocation can land in a bad RDNA4/Windows residency pocket even when kernel routes are unchanged | removes 3x+ prefill cliffs at large native ubatch | extra backend buffer chunks may add allocator fragmentation or affect non-ROCm backends if scoped incorrectly | single-chunk A/B with full `PP reserve` |
| H13 | RDNA4 MoE/MMQ LDS staging adaptation | Stormrage's RDNA2 MoE accelerator suggests MMQ prefill can benefit from explicit LDS staging, padding, and occupancy tuning; RDNA4 needs a separate gated variant rather than a direct RDNA2 port | +3% to +10% MoE prefill if current RX 9070 XT path is LDS/occupancy limited | wrong kernel route or RDNA4 occupancy regression; dense path regressions | opt-in RDNA4-only A/B on MoE `b=1024,ub=1024` with dense negative control |
| H30 | RDNA4 Q4_K/Q5_K large-prefill MMQ selector | Q4_K/Q5_K dequant+hipBLAS is much slower than MMQ on RX 9070 XT for Qwen3.6-27B-Q4_K_S at `ne11<=1024`, while the old RDNA4 K-quant MMQ gate was capped at `ne11<=192` | +3x to +4x Q4 prompt eval on affected prompt-heavy lanes | Q3_K/Q6_K regressions if broadened too far; Q4/Q5 shape-specific cliffs | Q4 pp512/pp1024 forced-MMQ A/B, default-threshold rerun, old-threshold negative control, Q3 negative control |
| H31 | RDNA4 Vulkan Q3_K coopmat prefill | Kept code is E082 stride18 + E086 corrected Q3_K `LOAD_VEC_A=4`; E102 now auto-enables AMD large matmul on the local eligible AMD proprietary coopmat device. E257 established the current dense 27B 12k profile at `b7168/ub1024`; E258 rejected Q3_K transpose-A storage, E259 rejected `b7680` and f16 KV default, E260 rejected graphics queue, `--no-mmap`, `b8192`, and broad `GGML_VK_DISABLE_F16=1` transfer gates, and E264 rejected graph-level FFN F16 src1 casts. E097-E101 also closed invalid/negative nearby routes: `wn48/wn96`, `bn256` large tiles, Q8_1/int-dot, aligned-store cleanup, and Q3_K arithmetic microforms. For the 130k D012 lane, D030 rejects extending the old q3quad/tile stack, scale-only helpers, signed-nibble-only storage, Q8_1/int-dot, expanded layouts, and neighboring tile tweaks as first `2.4 TPS` routes; D031 rejects compact Q3S/signed-nibble plus predecoded-scale layout-body work because static savings are too small, runtime evidence is negative, and residency cost is high; D032 shows FA-only cannot carry the target because FA `1.5x` still leaves Q3 `1.1987x` local needed; D033 rejects q3-octa/`LOAD_VEC_A=8` as an E087 repeat; D034 rejects memory-priority/pageable and backend-host KV placement as speed routes because the best partial-KV recovery is `1.9826 TPS`, still below D012 with decode regressed | target-closing work needs about `1.260x` local on all-Q3 or `1175 ms` D012 all-Q3 point savings; with FA stack, Q3 still needs about `1.18-1.20x` local. The next route must be a true Q3_K compute body or compressed-dot route, not layout-only unpack simplification, wider per-invocation dequant, or KV/output placement relief | invalid tile shapes can look fast by skipping output work; large LDS/register variants, expression-only helper rewrites, nearby launch/KV/batch/f16 toggles, transpose-A storage, per-layer activation casts, signed-nibble-only storage, compact Q3S layout-body, q3-octa/`LOAD_VEC_A=8`, Q8_1/int-dot, scale-only metadata, FA-only pivots, output placement, memory priority, and backend-host KV are exhausted | run `python scripts/research/vulkan_q3k_prebuild_gate.py --candidate "..."`, `python scripts/research/vulkan_warptile_static_scout.py`, `python scripts/research/spirv_op_summary.py <spv>`, and D030-D034-specific resource/residency gates before any new shader/tile probe |
| H32 | Vulkan Q3_K MMQ byte-wise sub4 | Q3_K MMVQ already subtracts the offset with a 32-bit byte-wise bit trick, but Q3_K MMQ still does unpack/subtract/repack in the hot dot loop that dominates prefill | +1% to +4% prompt eval if ALU/register overhead matters | compiler already optimizes it away, or bit-twiddle increases pressure | active-lane E072 `wm32-wn32` A/B with perf logger if promising |
| H33 | Vulkan Q3_K MMQ packed32 loads | Q3_K MMQ still loads four packed16 pairs for each repack even though Q3_K packed32 layout exists and MMVQ already uses it | +1% to +5% prompt eval if load/repack overhead is material | added temporaries/register pressure, or old packed16 loads already coalesce well | active-lane E073 `wm32-wn32` A/B |
| H34 | Q3 12k f16 KV decode profile | At ctx=12288 the Q3_K_S model fits with f16 KV; post-driver E116 shows this is useful on Vulkan decode-heavy generation, while E117 rejects it as a ROCm prompt-heavy replacement | +30% decode-route win vs ROCm q4 control when switching backend/KV together; small q4->f16 Vulkan edge | more VRAM, output-length/wall-TPS comparability changes, not viable for all prompt-heavy contexts | E116 Vulkan f16 decode r3 + E118 live-server sanity; E117 ROCm prompt-heavy negative control |
| H35 | ROCm Q3_K direct/persistent prefill route | Current large ROCm prefill already uses hipBLAS after Q3_K -> fp16 staging; E103 proves repeated staging exists, but E104 rejected persistent fp16 cache due VRAM residency pressure and E105 rejected selector-only routing to existing MMQ. E191/E192 refresh the route after the failed E190 MMVQ pair-dot: real-context `MUL_MAT forward` is dominated by `cublas_backend q3_K` (`3891.530 ms`, `78.70%`), and the current Q3_K split is `src0_convert_ms=1637.070`, `src1_ms=364.309`, `gemm_ms=3203.883`. A useful new route now likely requires a shape-specific non-persistent fused/direct Q3_K x F16 RDNA4 kernel, or graph scheduling that avoids repeated source staging without broad fp16 residency | +1% to +3% wall for `20-30%` local Q3_K conversion/layout win; higher only if fused route also improves GEMM-side locality | fused kernel losing to hipBLAS GEMM, RDNA4 tiling complexity, graph-scheduling complexity, or insufficient local share after conversion removal | Start from E192 split and E104/E105 negatives; do not repeat persistent fp16 cache, existing-MMQ selector probes, compute16, hipBLASLt, or ubatch sweeps |
| H36 | Prompt-cache/checkpoint repeated-session route | Sequential repo tasks share a large prefix; server prompt cache and context checkpoints can restore the common prefix and avoid most repeated prefill work; E113 shows shorter ngram can stack when repeated-session coverage appears | +20% to +70% repeated/session TPS, zero cold-first gain; post-driver stacked `ngram-mod 12/16/32` reached `19.5051 TPS` | misleading if compared to cold-first claims; depends on prompt similarity and cache memory; ngram benefit is bursty and first cold request can slow down | E111 same-lane reuse A/B; E112 reuse+ngram 24/48/64; E113 post-driver reuse+ngram 12/16/32 |
| H37 | Vulkan `-ngl 0` CPU fallback route | E125 shows Vulkan `-ngl 0` is a split route: model layers stay on CPU, but op-offload still uses Vulkan for prompt graph pieces. Decode remains CPU `Q3_K -> Q8_K` vec-dot bound and Q3_K has no x86 repack route. | +5% to +8% no-code via `--no-mmap`; larger gains require Q3_K x86 repack/interleaved vec-dot or partial layer offload | mmap/session noise can masquerade as kernel speed; disabling op-offload destroys prompt TPS; CPU code probes are slow to validate | Keep E125 `--no-mmap`; use small real-server benches; inspect `ggml_vec_dot_q3_K_q8_K`, type traits, and repack coverage before code work |
| H38 | Vulkan 64k long-context prefill route | E128 shows Vulkan wins decode but loses full `ctx=65536` wall because long prompt eval dominates. Perf trace attributes about `85.8%` of Vulkan time to `MUL_MAT q3_K` and `FLASH_ATTN_EXT`, so decode/speculative changes cannot close this lane. E131 maps the active FA route as coopmat1 q4/q4 `Br=16,Bc=64,D_split=8,row_split=4`, main `N=1024`, `KV=1024..57344`, `split_k=1`, `use_mask_opt=1`; E132 records resource stats `98 VGPR / 76 SGPR / 26112 B LDS / 0 scratch`; E133 narrows Q3_K to the top shapes `m=17408,n=1024,k=5120` and `m=5120,n=1024,k=17408` (`74.1%` of parsed Q3_K time); E134 adds route ceilings: FFN gate/up alone needs `2.234x` local, all Q3_K needs `1.357x`, FA alone needs `1.494x`, and Q3_K+FA together need `1.172x` local to match the ROCm 64k wall; E135 proves the real server graph exposes `63 x q3_K SWIGLU` FFN gate/up candidates with `m=17408,n=1024,k=5120`; E136 models dual-A/same-B FFN fusion and projects only `1.4466 TPS` under a generous A-proxy ceiling; E137 rejects a dual-N/same-A `mul_mm.comp` prototype because it raises accumulator pressure to `120 VGPR` and regresses pp7488; E138 rejects forced FA split-k because the existing reduce route drops prompt `666.87 -> 96.29 tok/s`; E139 rejects the existing per-node Q3_K predequant fallback because `Q3_K -> fp16 prealloc_x -> f16 matmul` drops pp7488 `969.61 -> 743.65` even though the f16 matmul pipeline uses only `77 VGPR`; E140 rejects existing matmul split-K for the hot reverse Q3_K shape because split-K2/4 measured `966.21`/`964.46` vs `968.74`; E141 rejects f16/q8 KV route pivots; E142 rejects larger-Br FA cm1; E143 rejects the large-N Q3_K warptile family because `BN192/BN256` reduce A-dequant proxy but raise VGPR/LDS/scratch enough to regress pp7488; E144 rejects BK16 because it lowers resources but doubles K-loop/barrier cadence and drops pp7488 `972.77 -> 587.52`; E145 rejects simple FA `D_split=4/16` because it keeps the same reported resources but regresses pp7488 `978.88 -> 953.24/951.54`; E146 rejects BM256 because it halves B/workgroup proxy but regresses pp7488 `972.84 -> 916.62` at `31744 B LDS`; E147 rejects broad persistent fp16/int8 Q3_K layout on memory and downgrades signed-nibble layout to low-confidence until instruction/resource proof improves. | +4% no-code route already found; larger gains need a route stack across Q3_K large-prefill and/or structural q4 FlashAttention long-KV work | 64k benches are slow, q4 V cache requires FlashAttention, invalid/corrupt Vulkan tile routes can look fast if coverage is broken, `max_tokens=1` screens can overstate tiny FA changes, support gates can silently fall back to scalar FA, launch-only fusion can look attractive despite low ceiling, graph-level FFN fusion without A-side work reduction is below the target, reducing repeated A work by doubling accumulators or larger N tiles can lose to VGPR/LDS/occupancy, reducing resources by shrinking BK can lose to barrier/K-loop cadence, FA split/reduce can serialize every FA node, existing per-node Q3_K predequant can lose to temp traffic plus sync even when the replacement matmul has lower VGPR pressure, matmul split-K can be neutral/negative when the shape already has enough workgroups, FA `D_split` can regress even when pipeline stats stay unchanged, larger M tiles can lose to near-limit LDS despite lower VGPR, and persistent Q3_K layouts can exceed VRAM or only remove low-ceiling bit-unpack work | Use E128 best `GGML_VK_ALLOW_GRAPHICS_QUEUE=1`, `--no-mmap`, `b8192/ub1024` as Vulkan baseline. E129 rejects simple FA `Bc` retuning, E130 rejects nearby `ub=768/1280`, E131 rejects FA mask-opt disable/f16acc, E132 rejects SHMEM staging fallback, E137 rejects current dual-N Q3_K, E138 rejects forced FA split-k, E139 rejects existing per-node Q3_K predequant, E140 rejects existing matmul split-K for `k=17408`, E141 rejects f16/q8 KV pivots, E142 rejects larger-Br FA, E143 rejects current large-N Q3_K warptiles, E144 rejects BK-depth shrink, E145 rejects FA `D_split` flips, E146 rejects BM256, and E147 rejects broad Q3_K persistent fp16/int8 layout. Next probes need route-ceiling gate, graph/resource proof, shape-level perf proof, and A-dequant/layout proof; Q3_K work should wait for a topology that removes A pair count without fp16 temp, accumulator blowup, near-limit LDS, or extra reduce, while FA work should move to shader-body/per-KV-tail redesign instead of cm1 tuning flips |
| H39 | ROCm decode parity with Vulkan | E061/E116 show a real short-decode gap after the driver update: ROCm q4 decode-focused route is about `29.1685 TPS` / `29.625 tok/s`, while Vulkan q4/f16 reaches `39.8801-40.2753 TPS` / `40.8683-41.2283 tok/s`. E149 audits the proposed Vulkan-to-ROCm plan and corrects the main premise: ROCm already has RMS, rope/set-rows, unary, SSM, FFN decode fusions and HIP graph capture, so the gap should not be attributed to "no fusion" or generic pipeline caching without trace proof. Fresh E149 ROCm non-sync trace is matmul-dominated (`MUL_MAT forward 77.84%`, `RMS_NORM+ROPE+SET_ROWS 3.26%`) with Q3_K direct route counts (`mul_mat_vec_q_direct,q3_K 929`, `mul_mat_q_direct,q3_K 349`); sync companion moderates that to `MUL_MAT forward+fused 53.80%`, `RMS_NORM+ROPE+SET_ROWS 11.83%`, `FLASH_ATTN_EXT 1.46%`, still pointing first at Q3_K matvec rather than standalone fusion. Graph-disable sequential control tied/noise. E149 Vulkan q4 perf comparator is also Q3-led (`MUL_MAT_VEC q3_K 50.67%`, `MUL_MAT_ADD_VEC q3_K 19.38%`) with tiny `ROPE+SET_ROWS 0.60%`. The first route-delta table finds ROCm decode-only Q3_K split as `mul_mat_vec_q_fused 63.78%` / direct `36.22%`; top aligned shapes are `m=17408,n=1,k=5120`, `m=5120,n=1,k=17408`, `m=10240,n=1,k=5120`, and `m=6144,n=1,k=5120`. E150 rejects disabling ROCm fusion (`30.08 -> 28.61 tok/s` decode), so the branch is optimize fused Q3_K MMVQ rather than remove fusion. E151 keeps RDNA4 `Q3_K/ncols_dst=1` `nwarps=2`: clean post-rebuild r3 `29.77 tok/s` decode, candidate r3 `32.2467 tok/s` (`+8.32%` decode, `+7.83%` aggregate); real `llama-server` sanity passed with normal thinking text. E196 refreshes the post-driver gap at ROCm r3 `31.9233 TPS` / decode `32.3833 tok/s` vs Vulkan r3 `40.8007 TPS` / decode `41.795 tok/s` and confirms Q3_K is still the main route body. E197 rejects wave64/row-warp reduction-only transfer, E198 rejects standalone q8 activation caching despite real cache hits (`303/777`) because clean A/B was `31.9368 -> 31.8573 TPS`, E199 rejects quick Vulkan-style padded-layout transfers, and E200 maps the only viable padded-storage route as an env-gated correctness-first branch covering non-split storage, dequant, MMVQ, MMQ, then split/async/copy/view offsets. The 64k decode gap needs a separate FA/KV trace. | +10% to +28% decode eval if the dominant Q3_K decode route moves; E151 already captures the first `+8.32%`, but ROCm still needs about `1.278x` aggregate/decode-route speedup to match the E196 Vulkan comparator | Stale/pre-driver traces can mislead, trace-heavy runs are not clean speed baselines, Vulkan f16/q4 comparisons can mix KV effects, missing `RMS_NORM_MUL_ROPE` may not be active for Qwen M-RoPE, broad ROCm selector/runtime knobs are already heavily rejected, Q3_K warp policy must stay RDNA4/Qwen-local unless cross-device A/B proves otherwise, and storage-blind packed-load rewrites do not reproduce Vulkan's padded device layout. Do not repeat wave64/row-warp topology, q8 activation-cache-only probes, local Q3_K packed32 load patches, or real-server speed runs before padded-storage correctness passes | Next: larger structural Q3_K route-body/layout work only. It must reduce real Q3_K dot/dequant traffic or create a new route. A full ROCm padded-storage route must begin with backend storage/correctness and `test-backend-ops` Q3_K `MUL_MAT`, while smaller probes must not just alter reduction shape, static branches, graph-local q8 staging, or storage-blind loads |
| H40 | DFlash integration track (Bee port to this fork) | BeeLlama reports large decode/repeated-task gains with `--spec-type dflash`, but implementation is a subsystem (draft architecture, hidden capture ring, server adaptive verify, backend hooks), not a simple kernel tweak. This fork already has strong speculative infrastructure (`draft/mtp/ngram`) that can host a staged DFlash port with lower integration risk if done phase-by-phase. | no immediate TPS claim; target is functional DFlash mode first, then repeated/decode-heavy gains on ROCm lane | scope explosion, CUDA-first assumptions vs local ROCm target, graph-reuse and capture-offset correctness risks, misleading cold-first claims | execute the staged plan in `docs/research/DFLASH_IMPLEMENTATION_PREP.md`; start with Phase-0 CLI/state skeleton and fail-closed behavior before backend hooks |
| H41 | ROCm transient same-step Q3_K staging reuse | E103 proved repeated Q3_K staging exists, but E104 rejected persistent fp16 cache because the working set is too wide. The missing middle ground is ephemeral reuse inside one graph step: sibling FFN/attention matmuls may touch the same `src0` range close enough in launch order that one temporary fp16 staging can be reused and dropped before residency becomes a problem. | +1% to +4% wall if a meaningful part of `src0_convert_ms` is removed without extending temp lifetime across the whole prompt | repeated keys may still be too far apart in launch order, or temporary reuse may add graph/sync complexity that eats the gain | extend E103 route trace with event-gap / temporal-locality metrics first; only build a transient reuse prototype if repeated keys cluster tightly enough |
| H42 | ROCm hot-shape Q3_K x F16 route | E192 shows the large-Q3_K cublas split is not just conversion-bound (`src0_convert_ms=1637.070`, `gemm_ms=3203.883`). E204 proves the hot-shape activation surface is real but rejects the current MMQ body as a speed vehicle, and E206 rejects cublas row-chunking because `6144x5120@2048` and `17408x5120@2048` both regress point timing. E244 shows a weight-major/multi-chunk GEMM schedule has a real but moderate standalone signal (`~5-7%` GEMM-local, optimistic `~1.076x` wall ceiling before overhead), so it is a stack component rather than the standalone +20% route. E245 rejects direct scalar/WMMA Q3FlashMatmul bodies. E246 finds a point-positive pivot (`Q3_K` chunked dequant feeding rocBLAS improves hot points by `1.17x-1.30x`) but the real-server runtime prototype regresses or times out once stream/pool safety is enforced. E248 finds the first runtime-positive side-route in this branch: adjacent-only `src1` fp16 reuse improves the active lane `7.8981 -> 7.9608` aggregate TPS in final3 (`+0.79%`). | +0.5% to +1.5% wall for safe adjacent activation reuse now; larger H42 still needs a new graph-safe Q3_K scheduling/body route | streaming Q3_K pipeline point wins can vanish under per-matmul sync, async auxiliary streams can create backlog/lifetime hazards, and activation reuse has a small ceiling | keep E248 opt-in, not default; do not promote E246 runtime pipeline; next large route must avoid per-matmul host sync and broad fp16 residency, and must prove graph-safe async lifetime before wall A/B |
| H43 | ROCm backend-private padded Q3_K storage contract | E199/E200 show Vulkan-like packed32/padded behavior cannot be transferred by a load-side helper patch while ROCm storage still uses raw `110`-byte Q3_K blocks. E201-P1a validated `raw Q3_K -> padded block -> fp16 dequant`; E201-P1b/P1c validated default-off non-split padded storage plus dense Q3_K MMVQ; E201-P2a adds padded-aware Q3_K MMQ loading and confirms a real short-lane win (`30.2390 -> 30.9884 TPS` r3, decode `31.1767 -> 31.9167`, Q3_K MMQ point `252.526 -> 231.453 ms`). E213/E216/E219/E220 hardened layout safety and fail-closed view handling. E221 initially rejected default-on, E222 closed MMVQ env-vs-layout divergence, and E223 successfully rolled out HIP default-on with explicit opt-out across backend/MMQ. | small but stable lane benefit as default now: 12k A/B `7.20 -> 7.25` (`+0.69%`) and 32k A/B `11.03 -> 11.07` (`+0.36%`) in r1, with full Q3_K broad smoke pass | residual risk remains in future unrelated refactors around storage semantics, split/view paths, or MoE routing; large `ncols=2048` prefill bottleneck remains H42 territory | completed and promoted: HIP default is now padded-on for Q3_K storage/MMQ, with explicit fallback via `GGML_CUDA_Q3K_PADDED_STORAGE=0 GGML_CUDA_Q3K_PADDED_STORAGE_MMQ=0`; continue H42 for large-prefill route gains |
| H44 | q4 tool-calling compensation | D037 showed q8/q8 KV is not viable as a default speed route on the active 130k Vulkan/RDNA lane, but q4 may expose weaker long-context tool use. D038 made this measurable: q4/q4 baseline repeats were `2/4` full pass rate and `0/2` smoke pass rate, with no invalid JSON and no unexpected tools; failures were missing tool emission under thinking and lost final grounding after valid tool results. D039 extends this to public BFCL-style data: explicit thinking on a 25-case BFCL-lite subset was `16/25`, while the default guard reached `24/25`. | measured quality/agent reliability improvement: default `LLAMA_SERVER_TOOL_CALL_THINKING_GUARD` behavior improved full D038 to `4/4`, smoke to `2/2`, and D039 BFCL-lite to `24/25` while staying on the same q4/q4 speed lane; set `LLAMA_SERVER_TOOL_CALL_THINKING_GUARD=0` or explicit request `enable_thinking=true` to disable/bypass for A/B | tool-call tests can overfit prompts; disabling thinking for all tool requests can reduce planning quality in complex workflows; BFCL-lite is not the official leaderboard runner; quality gains must not be reported as speed gains | keep the D038 guard default-on with env rollback. Next code step should be a smarter fallback/retry: preserve thinking when explicitly requested, retry after no-tool-call/length failures, and repair repeated parallel-call undercoverage like D039 `parallel_1`; compare against `d039-bfcl-lite-default25-r1` and `d038-toolcall-q4-thinkguard-r2` |
| H45 | Q4 C2 entropy-bounded nibble stream | D050-D052 show metadata-only cannot reach target13 for Qwen3.6-27B-Q4_K_S. A lossless entropy route over 4-bit symbols could lower effective payload bpw while preserving symbol semantics | potential path to the residual `0.631 GiB` gap when stacked with C1 | decode regularity and random-access overhead can erase net gain | theory-only: entropy atlas + page/header model + decode-cost budget before any prototype |
| H46 | Q4 C2 superblock palette remap with escapes | Many superblocks may use sparse local subsets of 16 nibble symbols; local remap+escape can reduce average payload bits while preserving exact values | medium-high compression on low-entropy/sparse-symbol tensors | flat distributions and escape-heavy tails can collapse gains | theory-only: active-symbol distribution + escape forecast + net bpw with side overhead |
| H47 | Q4 C2 tuple dictionary coding | Short nibble tuples may repeat enough for macro-symbol dictionary coding with literal fallback | moderate compression in tensors with strong tuple reuse | dictionary side-cost and miss locality can negate benefits | theory-only: tuple Zipf coverage + dictionary overhead gate |
| H48 | Q4 C2 layer-adaptive mixed policy | A single C2 method is unlikely to be globally optimal; per-tensor mixed selection can maximize compression under risk constraints | better risk-adjusted global bpw vs one-method policy | policy complexity and route fragmentation | theory-only: per-tensor ranking and mixed-policy global projection with strict fallback |
| H49 | Q4 C2 context-conditioned entropy pages | Replace flat-symbol coding by short-context conditional entropy pages with deterministic random-access anchors | potentially crosses corridor by reducing conditional entropy below H45 floor | context-model overhead can erase gain; branch cost may rise | theory-only: conditional entropy atlas + anchor/page overhead proof |
| H50 | Q4 C2 bounded-rANS deterministic micropages | Use bounded-state rANS on fixed micropages to raise coding efficiency while keeping decode state deterministic | near-corridor projection with tighter complexity than generic entropy coder | state/table traffic and page headers may offset bpw gains | theory-only: rANS table budget + state-transition complexity model |
| H51 | Q4 C2 superblock graph remap + selective literal lanes | Extend palette remap with graph-structured transitions and selective literal lanes to reduce escape inflation | could improve SPRE tails if transitions are predictable | compression projection currently above corridor under realistic headers | parked until stronger compression proof |
| H52 | Q4 C2 hierarchical tuple-context dictionary | Hybrid tuple+context dictionary to improve hit-rate beyond fixed tuple model | could reduce tuple literal fallback on structured tensors | complexity projection currently above hard decode budget | parked until complexity-reduction proof |
| H53 | Q4 C2 nibble reordering within superblocks | Sorting nibbles within blocks creates runs that lower conditional entropy | theoretical bpw reduction via permutation + order-1 coding | permutation overhead exceeds benefit; block boundaries dominate bigram count | rejected D063 (empirical delta=0, analytical 0 feasible) |
| H54 | Q4 quantization redesign for lower-entropy payloads | Change quantization to produce more structured/compressible nibble distributions through value-aware quantization or adaptive binning | potential path to reduce H1 below 3.8 bpw floor | scope, correctness, migration complexity | active: H54-B passed final analytical gate D070 and is authorized for guarded prototype |
| H55 | ROCm Windows dual-GPU split transfer path | E269 shows `-sm layer` baseline loses decode speed versus ROCm1-only because cross-device scheduler copies are host-staged while direct HIP peer-copy is unsafe on this setup | recover part or all of the `~10-16%` dual split decode gap | pinned staging may be noise-level, async staging can worsen scheduler syncs, direct peer-copy currently corrupts generation | E270 pinned host-stage A/B, copy-path timing, and peer-copy correctness microtest before any default peer-copy change |
| H56 | Vulkan dual-GPU MTP placement and prefill window | On MTP-enabled GGUFs, layer-split places `blk.*.nextn.*` on the second physical GPU and Vulkan pipeline parallelism creates extra scheduler copies; long-prompt MTP also overpays target NextN extraction when the prefill tail window is too large | recover MTP decode gain on Vulkan dual while keeping long-prompt prefill at or above baseline | deeper draft depths can still be dominated by repeated draft decodes; disabling pipeline parallelism may not help every Vulkan topology | E274 nextn-to-primary placement, Vulkan NextN PP guard, `LLAMA_SPEC_PREFILL_WINDOW=512` default, and n2/n3/n4 sweep |
| H57 | Exact MTP prefill-window batch boundary | The server enables a 512-token MTP tail for the entire physical batch that contains the boundary; with `b=8192`, the measured final prompt batch was 6206 tokens, so target NextN extraction and draft-context catch-up run on thousands of unnecessary rows | recover the MTP prompt-eval regression on both ROCm and Vulkan while preserving the 512-token drafter history | splitting a mixed/multi-slot server batch incorrectly could reorder tokens or change non-MTP scheduling | E275 split the physical prompt batch exactly at the MTP window boundary, then trace target and draft ubatch sizes before/after |
| H58 | RDNA4 small-N MMQ gate/up activation reuse | MTP verify sends `N=2..5` Q3_K FFN gate/up pairs through MMQ; both matrices consume the same f32 activation, but each call independently quantizes it to Q8_1 | remove one Q8_1 conversion per dense FFN pair and reduce the measured `~65-90 ms` target verify body without changing acceptance | a persistent cache can reuse stale activations (E253); graph fusion may not materialize on split buffers; conversion-only savings cannot reach 1.6x by itself | E276 activated correctly but tied control under game contention (`-0.43%` aggregate); source prototype reverted, clean rerun optional |
| H59 | RDNA4 Q3_K small-N DP4A MMQ route | RDNA4 MTP verify uses `N=2..5`, but the current WMMA body has a fixed 16-column tile and computes 68.75-87.5% padded columns | use a dedicated Q3_K DP4A load/dot/writeback specialization with `mmq_x=4` for `N<=4`, reducing padded work and LDS while preserving the existing WMMA route for larger N | N=5 needs 50% of WMMA effective throughput and did not pay back; DP4A changes accumulation order slightly | D078 kept: n3 decode `34.92 -> 41.25` (`+18.14%`) and `1.65x` spec-none on the short lane; default for N=2..4, N=5 remains opt-in |
| H60 | Remaining ROCm MTP speculative overhead after D078 | D078 reaches `1.65x` short decode but the coverage-aware model still backsolves about `0.61` combined overhead; at 131k the gain falls to `1.41x` as FA/KV share grows | separate target verify, draft-context, FA/KV, scheduler sync, and host-staged dual-GPU transfer time before choosing the next body | synchronized traces perturb dispatch; one center may not dominate; Windows peer-copy remains unsafe | E277 rejected deferred accepted-prefix catch-up (`+0.42%` noise): `process` mostly waits for required target NextN output; next trace must split target Q3_K from FA/KV and cross-device work |
| H61 | Vulkan long-KV MTP first-verify cold graph | GUI autotune used only 16 output tokens; first N3/N4 target verify costs about `630 ms`, then falls to `42-49 ms`, making MTP appear slower than none | use a 128-token honest autotune lane and separately trace the PP-to-TG first-graph transition | exact pre-warmups do not survive long prefill; keeping PP buffers helps only partially | E278 kept GUI max_tokens=128 and phase trace; same-prompt Vulkan MTP n3 beats none `38.78 vs 29.15` (`1.33x`); post-MTP warmup reverted |
| H62 | Batched recurrent checkpoint transfer | Qwen3.6 hybrid memory writes one `149.6 MiB` checkpoint through 96 R/S tensor reads; direct incremental timing found `33.36 ms` of real transfer while the earlier `0.9-1.1 s` estimate mixed checkpoint work with pending prompt compute | batch checkpoint reads per Vulkan backend; separately bound GUI prompt-cache/checkpoint RAM | incorrect synchronization can corrupt recurrent rollback; large staging allocations can increase Host memory; checkpoint work is not the main 98k bottleneck | E279 kept: transfer `33.36 -> 27.38 ms`, 15-token incremental prompt `180.55 -> 165.79 ms` (`+8.9% TPS`), deterministic rollback output identical; long first-prefill change is neutral |
| H63 | Vulkan small-N long-KV FlashAttention | At 98k reused context, 39-40 new prompt tokens run at `~34 tok/s`, close to decode, while 2k-4k tails retain `315-425 tok/s`; after E279 the missing time is primarily small-query attention over the long q8 KV rather than checkpoint serialization | specialize or retile the Vulkan FA route for `N=16..256` with `KV=64k..128k`, reducing dispatch and KV-read overhead without changing attention semantics | the workload may already be bandwidth-bound; q4 KV can look faster while changing quality; trace synchronization can distort small-N kernels | fixed-prefix/fixed-tail perf trace at N=16/40/128/256 and KV=32k/64k/98k, then compare active FA shader/tile and q8/q4 negative controls |
| H64 | Vulkan dual-GPU primary/output placement | In layer split, KV follows each model layer, while first-device compute and the default last-device output tensor create avoidable pressure on display-attached GPU0 | use `-dev Vulkan1,Vulkan0 -sm layer -ts 1,1` plus `LLAMA_OUTPUT_DEVICE=Vulkan1`; retain `LLAMA_KV_DEVICE=Vulkan1` only as an explicit residency diagnostic | forcing all KV to one GPU creates repeated cross-device attention transfers; uneven layer splits reduce prefill throughput | E280 keeps output relocation: GPU0 accounted memory `8586 -> 7582 MiB`, prompt `1860 -> 1794 tok/s` r1. All-KV moved the full `4352 MiB` cache to GPU1 but collapsed prompt to `815 tok/s`, so it is not a default solution |
| H65 | Dual-RDNA4 Q4 long-prompt 1500 tok/s route | The new MTP-enabled Q4_K_M and UD-Q4_K_XL models fit statically across two 16 GiB GPUs, but the old Q4 result predates the motherboard and current placement/runtime fixes. A fresh same-lane A/B selected Vulkan XL before tracing. | measured Vulkan XL `1182.76 prompt tok/s` at 56,456 tokens; reaching 1500 requires `1.2682x` | WDDM Shared GPU Memory rises during active large-prompt compute despite nominal startup headroom; current tensor split is also about 4-5x too slow and lacks quantized-KV support | stopped E281 at the residency gate before trace/source work; resume only with more dedicated VRAM or an explicitly changed residency contract |
| H66 | Dual-Vulkan Q3_K_S 2000 tok/s long-prompt route | The non-MTP Q3_K_S model leaves substantially more VRAM headroom than Q4. D079 measured Q3_K at 46.6% and FA at 46.4% of parsed time; D080 then balanced the layer pipeline with `-ts 5,6`. | current cold-first best `1350.01 prompt tok/s` at 56,456 tokens; target `2000` still requires `1.4815x` | old D029-D033 fences close helper/layout-only Q3 routes; Vulkan tensor split reached only 29.9% of layer speed and performs 127 host-fallback allreduces per ubatch; Q3_K and long-KV FA must improve together | keep D080 `-ts 5,6`; continue P003 with a true Q3_K+FA body/dataflow route, not tensor split or ratio sweeps |
| H67 | Dual-Vulkan Q3_K BN512 12k prompt route | The reopened 12k lane measures Q3_K at 67.79% of parsed prompt time. Current accepted `BN=256` uses 82 VGPR and 31,744 B LDS; extending only the N tile to 512 prepares a 1024-thread workgroup with an estimated 54,272 B LDS. | rejected: runtime could not select the over-LDS hot route and measured only `950.35 tok/s` versus `1821.13` | 54 KiB LDS exceeds the usable current route limit | D082 rejected and runtime prototype removed |
| H68 | Native Vulkan tensor all-reduce | Replace Meta's generic tensor reduction with backend-native parallel transfers, BF16 compression, and AVX2 host reduction | improved tensor prompt from about `540` to `1032-1043 tok/s` | Windows exposes two singleton Vulkan device groups; 127 standard TP collectives remain host-mediated | D084 keeps opt-in infrastructure but rejects tensor as the P003 default; reopen only with true peer/device-group transport |
| H70 | Vulkan Q4_K_M large-prompt prompt-eval supremacy | Closed 2026-08-13 after D094 cycles 1-7: FA/concat/int8-coopmat scoped candidates exhausted; no supremacy claim | accepted: `GGML_VK_ALLOW_GRAPHICS_QUEUE=1` = `1241.31` (+3.31%, r3 stable) and cycle-6 q8 pre-dequant staging | native FP8, mixed K/V and MTP-native follow-up transferred to D096 | D094 closure; reopen only for a new broad prompt topology with adjacent ROCm control |
| H71 | ROCm FP8 production hardening | D098 proved native full FP8 FA for the Qwen D256/gfx1201 lane, but alternate head sizes, older AMD targets, mixed K/V types and exact K/V aliases were deliberately fail-closed | accepted: native KQ D64/80/96/112/128/256; native V D64/128/256; portable fallback elsewhere; mixed K/V and exact aliases covered | `19/19` ROCm0 focused cases, gfx1201 build, compile-only gfx1100 build; 49K FP8 `1809.62/22.96/9.0811` vs exact q8 center `1740.51/21.94/8.7217` | D099 complete; lower-wave D80/D96/D112 native-V variants remain research-only and disabled by default |
| H72 | ROCm long-context execution gap | Closed 2026-08-13 after D100: graph replay is stable and steady host launch is about `0.6 ms` per graph, while N=1 device graphs cost `18.829/19.642 ms` at 49K and `21.329/22.192 ms` at 98K | accepted observability only: asynchronous whole-graph device timing, launch-geometry trace and summary tooling | 16-wave, unsupported 8-column rocWMMA, and forced 32/4 KV-slice variants all failed the `>=3%` gate; unsafe peer-copy and broad graph refactors remain excluded | D100 complete; reopen only for a new device dataflow/kernel mechanism with a modeled `>=3%` ceiling |
| H73 | ROCm full-FP8 long-KV phase ceiling | Closed 2026-08-14 after D101: full-native center `22.815 tok/s` beats KQ-only `19.60` and portable reference `17.31`; both native phases are material wins | no production change; a shared-scale combine prototype passed correctness but tied the `22.865` control center at `22.91` (`+0.20%`) | rollback routes lose `14.1-24.1%` decode; duplicated scalar combine work is not on the token critical path | D101 complete; successor must measure or change dataflow inside the main fused long-KV body |
| H74 | ROCm full-FP8 fused-body phase census | Closed 2026-08-14 after D102: the N=1 fused decode kernel spends `55.4%` in P*V, `28.3%` in KQ, `14.3%` in softmax+P requant, `2.0%` in the merge, identical on both devices | the P*V phase misses the 60% single-phase gate, so the body is jointly MMA/LDS/bandwidth bound and no prototype is admitted | the census variant is env-gated and template-bounded; production bytes are unchanged; the pinned-mirror write/fence distorts the census run only | D102 complete; the fp32 VKQ-part LDS store/reload inside the P*V phase is the open lever (H75) |
| H75 | ROCm P*V phase instruction/register cost in the native decode body | D102 measured P*V at `55.4%` of the decode kernel while KQ (same tile reads and MMA count per chunk) takes `28.3%`; D098 proved LDS bandwidth is not the limit and registers are (f16 store lost 6.5% on +32 VGPR) | model whether the fp32 VKQ materialization can be fused into the final store without extra registers, then one bounded prototype validated on the 98K lane | decode-share math: the FA kernel is ~20% of a 49K token, so a 10% P*V cut is worth ~1.1% decode at 49K and ~2.2% at 98K; the 49K 3% gate needs a ~30% phase cut | deprioritized with D103, superseded by R001 |
| H76 | R001: CUDA-centric assumptions in ggml-cuda cost cycles on gfx1201 | ggml-cuda was designed for NVIDIA (32-thread warps, NVIDIA bank padding, cp.async, tensor-core layouts, Ampere occupancy); the HIP path compiles the same sources onto RDNA4 | audit each assumption against the extracted gfx1201 ISA (W00-W9) and quantify the shim cost before admitting any candidate | ISA-only phase first (no builds/benches per R001 phase 1; GPUs reserved for the subproject); candidates must clear the 3% decode gate on the locked lane | R001 active, phase 1; W01/W02 evidence recorded 2026-08-14: 73% s_wait_alu stream, scalar u8 tile loads, VOPD unavailable (wave64) |
| H77 | R001: occupancy of the decode kernel - LDS-bound at 2 CTAs per CU | kernels are wave32 (W01 empirical); 156 VGPR -> LLVM model gives 6 waves/SIMD = 24/CU, so even 3 CTAs fit the VGPR file; LDS 29,568 B x 3 > 64 KiB, so **2 CTAs per CU is already achieved and LDS is the binding constraint** | CLOSED-REJECTED 2026-08-14: (a) the decode grid is at most (1, 8, 24) = 192 blocks = exactly 2 CTAs/CU on 96 CUs - a 3rd CTA would sit idle during decode (fattn-wmma-f16.cu:42); (b) the register-acc variant that cut LDS to 21,120 B pushed VGPR 156 -> 181 (5 waves/SIMD) and regressed decode -3.8% (21.49 vs 22.33 t/s) | 3 CTA can only matter for prefill-sized grids, and there the same register-acc variant regressed prefill -3.7% too | R001 W08; measured in phase 2 (2.4) |
| H78 | R001: K/V tile streaming uses no async copy and should ride the scalar path | W02 confirmed: no cp.async and no vectorized tile loads - K/V fp8 arrive as per-lane scalar global_load_u8 (256 static in the production kernel), Q as u16; no s_load_dwordx16 tile staging | vectorized loads (global_load_b128) + ds_swizzle redistribution, or SGPR-based staging, could cut the load instruction stream and address-VGPR pressure | redistribution into WMMA fragments costs LDS traffic + permutes; benchmark the trade-off; pipelining bounded by LDS budget | R001 active; candidate on the phase-2 shelf |
| H79 | R001: PV phase costs 2x KQ per WMMA because its B-fragments come from LDS | PREMISE CORRECTED 2026-08-14: the production source preloads all P_f8 B-fragments (KQ_b) into registers before the V loop, and the disasm confirms the 16 ds_load_u16 are batched (median 226 instrs from the next wmma) - B is already in registers, like KQ's Q_b | remaining per-chain operand fed from memory is V_a (scalar u8 global loads interleaved with the mma chain); phase-2 candidate prefetches V_a one chain ahead (built, A/B on the 49K lane) | if the prefetch is neutral, the 2x gap is accumulator-chain hazard structure + tail, not load latency | R001 W03 (corrected), 2.2 in PHASE2_PLAN.md |
| H80 | R001: KV loads should be marked streaming in L2 at long context | KV = 128 KiB/token (64 layers x 4 heads x 256 x 2 x fp8); at 49K each GPU re-reads 3 GiB of KV per decode token vs 64 MB L2; the KV stream is use-once per token and displaces the weight-stream working set; the disasm shows no cache-policy modifiers on the KV u8 loads (W09) | BLOCKED by toolchain (tested 2026-08-14): __builtin_nontemporal_load is silently dropped by ROCm 7.1 clang (byte-identical encodings), and llvm-mc (AOMP-18) parses no glc/slc/nt/cache_policy/th modifier for global_load | retry when the toolchain can express the gfx12 TH (temporal-hint) field; ISA Table 13: TH=1 = NT for near+far caches | R001 W09, 2.1 closed-blocked in PHASE2_PLAN.md |
| H81 | RPC-first inter-ubatch prefill pipeline | rf44/rf45 measure 221-238 ms/ubatch at the RPC0 to Vulkan0 `l_out-18` boundary; rf43 copy-slot rotation alone is only +0.4% because the next RPC graph is still submitted after the local tail | use two owned boundary slots and submit RPC head N+1 while Vulkan0/Vulkan1 consume N; scheduler thread alone performs Vulkan set/compute | graph and KV lifetime, slot reuse and RPC response ordering can corrupt state; detached workers must never touch Vulkan compute state | D132 active; P0 copy census and ubatch-fusion control first, then default-off P1/P2 |
| H82 | RPC Q8_0 activation wire | the RPC-first boundary is F32 `l_out-*`; its F16 wire is 10,485,760 bytes per 1024-token ubatch and remains on the 1 GbE critical path | block Q8_0 wire with parallel peer conversion, combined with existing prefill copy-slot rotation | lossy activation boundary requires a separate quality/PPL gate; protocol peers must fail closed | accepted RPC-only opt-in: D134 R2 repeats center `1287.445` prompt / `14.389` aggregate TPS, `+5.68%/+10.76%` vs F16; smoke acceptance stable; short PPL neutral (`4.9183 +/- 0.12282` F16 vs `4.9194 +/- 0.12287` Q8), keep default-off pending longer evidence |

## Recent H42 Negative Gates

- P002 low-level continuation: `docs/research/major-topology/D002_P002_ROCM_LOW_LEVEL_Q3K_BODY.md` opens a new ROCm Q3_K body gate for the 130k lane. It does not reopen E245 direct Q3FlashMatmul, E246 unsafe streaming runtime, or E249 hipBLASLt grouping; it requires a fresh ROCm P002 route trace plus standalone HIP point proof before runtime code.

- E228 recentered the cold-first 12k bottleneck: robust Q3_K split is now mostly GEMM-side (`2906.403 / 3783.195 ms`, `76.82%`), so src0/src1 staging-only routes have low ceiling.
- E229 rejected direct rocBLAS solution-index plumbing for the dominant shape: `(17408,2048,5120)` default remained best/tied, and the smaller confirmed wins project to only about `121 ms` before runtime overhead.
- E231 rejected a host/scheduler-first branch: graph build, alloc, and inputs were about `42 ms` total in a sync-timed `6525 ms` ubatch run.
- E232 rejected rocBLAS batched gate/up pairing: `rocblas_gemm_batched_ex` batch=2 was about `27x` slower than two separate default GEMMs on the hot/tail `17408x5120` shapes.
- E233 rejected transposed fp16 staging for rocBLAS: the hypothetical `A*B` contract was `3.5%` to `16.3%` slower than the current `A^T*B` contract before staging cost.
- E234 rejected current-compatible f16-output+convert: standalone GEMM had some f16-output signal, but runtime shifted cost into convert-back and narrow wall A/B regressed `7.89 -> 7.63 TPS`.
- E235 rejected broad FFN f16-intermediate fusion as the next route: the main `n=2048` FFN scout regressed `9.7029 -> 10.8515 ms`; tail `n=1382` improved but has too little solo ceiling and requires correctness-heavy graph work.
- E236 rejected f32-first secondary-route work: robust `cublas_backend/f32` is only `443.826 ms` in the E228 trace, and the broad SSM `ncols=2048/1382` shapes are not covered by current direct f32 kernels without a low-ceiling selector-only force.
- E237 rejected dense Q3_K reuse of the existing RDNA4 MMQ staging body: exact `17408x5120@2048` current-MMQ point regressed `2053.749 -> 2496.968 ms` while shared memory rose `40448 -> 61956 B`.
- E238 rejected graph-level FFN tall GEMM: standalone rocBLAS tall-pair timing looked positive, but the in-runtime point gate timed out and robust main `ncols=2048` tall route took `4304.43 ms` on `187` rows versus E217 current `1415.111 ms` on `189` pairs; the in-runtime GEMM alone regressed before GLU/output cost.
- E239 rejected a padded Q3_K dequant half2 store rewrite: correctness passed and `src0_ms` improved only `517.104 -> 495.805 ms` (`-4.12%`), below the point gate, while trace-context prompt eval tied/slightly regressed.
- E240 rejected the only targeted mid-ubatch recapture: `ubatch=3072` was viable but slower than E226 (`7.7050` vs `7.8890` TPS), so the `2048..4096` boundary is not the next speed route.
- E242 rejected concurrent rocBLAS stream pairing: the dominant `17408x5120@n2048` pair was slightly slower, and the tail-only win was too small for graph-pairing complexity.
- E243 rejected GDN warp-count/block-geometry tuning: `GGML_GDN_NUM_WARPS=2` improved synchronized GDN timing only `~1.5%`, below wall A/B threshold after E230's larger GDN point win failed to convert.
- E244 keeps multi-chunk/weight-major GEMM scheduling only as a stack clue: full-prompt `n=7489` GEMM is `~5-7%` faster than `3*n2048+n1345` on the main FFN forms, but the optimistic combined ceiling is only about `1.076x` wall before overhead and naive full-prompt intermediates risk residency cliffs.
- E245 rejects direct Q3FlashMatmul bodies: scalar P0 and RDNA4 WMMA P1/P2/P3 were correctness-clean after the B-fragment fix, but still far slower than `Q3_K -> fp16 -> rocBLAS`.
- E246 keeps the standalone point-positive clue, but rejects the current runtime pipeline: safe sync regressed and async timed out in the real server path.
- E248 initially looked like a small opt-in adjacent `src1` fp16 reuse cache: final3 aggregate `7.8981 -> 7.9608` TPS (`+0.79%`) and trace smoke `429` adjacent hits. E253 later failed to reproduce safe usefulness after rebuild: `batch=8192` reuse collapsed to `2.4701 TPS`, and even guarded revalidation at `batch=6144` measured only `2.5578 TPS`. Treat E248 as unstable/disabled for launch recommendations until a separate lifetime/scheduling fix exists.
- E249 rejects `hipBLASLt` grouped GEMM for the active FFN pair on ROCm 7.1 Windows: no grouped algorithms were exposed for the current f32-output contract or f16/fast16 variants.
- E250 rejects the local `Qwen3.6-27B-Q3_K_S_mtp.gguf` as a cold 10 TPS route: exact-shape `spec=none` and smaller-shape `spec=mtp` both hard-timed out before prompt eval completed.
- E251 rejects Q4 MTP fit-auto as a pragmatic cold escape: forcing full offload still fails fit, while omitting `-ngl` completes at only `1.17 TPS` because CPU/GPU placement dominates.
- E252 rejects the f32-output fast16 hipBLAS compute contract: `HIPBLAS_COMPUTE_32F_FAST_16F` for active `f16 x f16 -> f32` Q3_K GEMM returns `CUBLAS_STATUS_NOT_SUPPORTED` on ROCm 7.1 Windows, and the prototype knob was reverted.
- E253 rejects stacking E248 with `batch=8192` and reclassifies E248 as unstable after current rebuild; broad src1 reuse is not a launch/autotune recommendation.
- E255 confirms a practical local cold `>10 TPS` profile with `Qwen3.6-35B-A3B-UD-IQ3_XXS` (`22.1407 TPS` r3), but this is a model/profile result and not a dense 27B-Q3 speedup.
- E256 rejects intermediate dense 27B `ubatch` retuning (`1536/1792/2560` all below the latest `2048` clean control).
- Next H42 work should not promote E246 runtime pipeline or E248 src1 reuse; library selector, batching/grouping, naive row split, launch-count/block-geometry, A-layout, f16-output-with-convert, broad f16-intermediate FFN routes, f32-first secondary routing, dense reuse of the MoE/RDNA4 staging body, current hipBLAS/cublas tall-FFN pairing, staging-only padded-dequant store rewrites, GDN geometry, nearby ubatch recapture, direct Q3FlashMatmul scalar/WMMA bodies, naive full-prompt multi-chunk GEMM, unsafe per-matmul auxiliary-stream pipelines, the local Q3_MTP GGUF, Q4 MTP fit-auto, `32F_FAST_16F` hipBLAS compute, current src1-reuse stack, and intermediate ubatch retuning are now exhausted as standalone routes for this cold lane.

## Priority (Start Here)

P003 addendum (2026-05-28): H45-H48 are strictly theory-only at this stage.
Do not build converter/runtime prototypes for these hypotheses until the D053
analytical gates are completed.

P003 addendum (2026-05-28, D060): H49-H52 are also theory-only. Admission
screen currently allows analytical continuation for H49/H50 only; H51/H52 are
parked pre-gate.

P003 addendum (2026-05-28, D061/D062): first fast formulations are negative for
both admitted routes (H49 adjacent first-order conditional signal near zero;
H50 order-0 micropage model has zero feasible configs).

P003 addendum (2026-05-28, D063): H53 rejected both empirical and analytical
gates. Empirical test on 24 tensors showed `Hcond delta=0.000000 bpw` (block
boundaries dominate). Analytical gate tested 44 configs (11 block sizes × 4
encoding methods), 0 feasible under corridor `3.57-3.77 bpw`.

P003 addendum (2026-05-28, D067): H54-B (value-aware quantization) fast gate is
positive. Sampled run on 24 tensors (`6,291,456` elements) reports entropy
`3.870042 -> 3.267969 bpw` (`delta=-0.602073`) and feasible tensors `24/24`
under corridor upper bound `3.77`. This is a sampling gate only; full analytical
closure still requires broad/unsampled entropy pass, normalized quality delta,
and decode-complexity proxy before prototype authorization.

P003 addendum (2026-05-28, D068): representative spread sample confirms H54-B.
On 48 tensors (`12,582,912` sampled elements), entropy is
`3.865866 -> 3.277582 bpw` (`delta=-0.588283`), feasible tensors `48/48`, and
weighted `NRMSE=0.101195`. H54-B remains the active continuation route.

P003 addendum (2026-05-28, D069): wide all-Q4 screen confirms H54-B signal.
On 348 tensors (`45,613,056` sampled elements with per-tensor cap), entropy is
`3.864270 -> 3.277495 bpw` (`delta=-0.586775`), feasible tensors `348/348`, and
weighted `NRMSE=0.101327`.

P003 addendum (2026-05-28, D070): final analytical H54-B gate is PASS under
explicit triple-contract check:

- entropy `3.277495 <= 3.7701`,
- weighted `NRMSE=0.101327 <= 0.115`,
- complexity index `1.127917 <= 1.35`.

Status: H54-B is now authorized for guarded prototype (not default rollout).

P003 addendum (2026-05-28, D071): first guarded prototype planning artifact is
implemented. New script `q4_metacomp_guarded_prototype.py` combines C1
phase1-budget and H54-B value-aware wide artifact into a fail-closed per-tensor
manifest. For `Qwen3.6-27B-Q4_K_S`, projected size is `12.130 GiB` versus
target `13.000 GiB` (headroom `0.870 GiB`) under `nrmse<=0.115`, entropy-gain
threshold `>=0.45 bpw`, and C2 safety margin `0.90`.

P003 addendum (2026-05-28, D072): guarded runtime-sidecar MVP is implemented.
New script `q4_metacomp_guarded_runtime_sidecar.py` builds fail-closed sidecar
artifacts from D071 manifest (selected/fallback rows + runtime gate metadata).
Bounded MVP run (`64` selected rows) reports sampled selected entropy
`3.867974 -> 3.279216 bpw` (`delta=-0.588758`) and keeps runtime defaults
unchanged (opt-in env gate + explicit rollback only).

P003 addendum (2026-05-28, D073): guarded runtime reader is now integrated in
core C++ model load path (`llama-model-loader.cpp`) under strict opt-in gate.
When `LLAMA_Q4_METACOMP_ENABLE=1`, runtime reads sidecar path from
`LLAMA_Q4_METACOMP_SIDECAR`, parses selected tensor names, validates tensor
existence and Q4 type, and logs validation counters. Any env/path/parse/row
validation issue is fail-closed and falls back to the legacy path.

P003 addendum (2026-05-28, D074): first runtime-applied stage is now verified
under explicit env gate `LLAMA_Q4_METACOMP_FORCE_CPU_SELECTED=1`. With
validated sidecar rows (`128`), runtime reports `forced_cpu=128` and model
buffers shift from control `Vulkan0 11665.45 MiB / Vulkan_Host 3708.06 MiB`
to candidate `Vulkan0 8619.29 MiB / Vulkan_Host 6754.22 MiB`.
This proves applied runtime behavior (real residency change), but it is still
GPU->host redistribution, not true payload compression.

P003 addendum (2026-05-28, D075): first quality-safe storage prototype is now
implemented via `q4_metacomp_lossless_pack.py`. It builds a lossless packed
artifact for selected Q4 tensors (per-entry zlib payload + crc32/sha256
contracts + roundtrip verify). Smoke on 8 selected tensors shows raw
`1,066,106,880` -> packed `1,045,885,219` bytes (ratio `0.981032`, saved
`19.28 MiB`) with exact byte restoration. This is the correct quality-preserving
direction, but runtime decode/compute integration remains mandatory before any
VRAM/runtime win claim.

**Fundamental conclusion:** Current Q4 layout has `H1=3.864885 bpw`, leaving
only `~0.135 bpw` headroom before any overhead. All symbol-level C2 formulations
(H45-H53) fail to reach corridor. Shannon entropy permutation invariance blocks
rotation (H54-A). Next direction: **H54** — change quantization itself to produce
lower-entropy payloads. Do not pursue further symbol-level C2 variants on current
Q4 layout, or further rotation/permutation variants. Continue only with
redesigned quantization (H54-B/C/D) or mixed precision.

Current user focus (2026-05-24): H39 ROCm decode parity remains open, but E197 rejects the simple wave64/row-warp topology transfer, E198 rejects standalone q8 activation caching, E190 follow-up rejects helper-level pairdot on/off as a real limiter, and E199 rejects quick padded-layout transfers that do not change ROCm's underlying Q3_K storage contract. E201-P2a is the first padded-storage continuation with a real wall win: it keeps storage/MMQ default-off, passes Q3_K `MUL_MAT` correctness, improves Q3_K MMQ point timing, and confirms short-lane `+2.48%` r3. The next useful ROCm decode branch must still reduce real Q3_K dot/dequant work or change layout/route structure, not only reduction shape, static branches, graph-local q8 staging, storage-blind packed loads, or launch/cache plumbing. E191/E192 still show the practical real-context wall lane is H35/H42 large-Q3_K prefill dominated. Keep pure decode-parity experiments separate from repo-snapshot wall experiments. H38 remains documented and paused; do not mix its 64k prefill targets with the short-decode ROCm-vs-Vulkan target unless a separate long-context decode trace is being collected.

1. H11 is completed and kept: E008 confirms ROCm compute vbuffer chunking fixes the native `ub904/1024` residency cliff. E123 post-driver 12k scout found no new speed from retuning chunk size (`128 MiB`, `64 MiB`, and single-chunk all below E113), so allocator chunking is a guard/negative-control path, not the next speed source.
2. H08 remains useful for symptom triage, but caps/planners are now diagnostic tools rather than the preferred final fix when allocator layout can be repaired.
3. H02 because it can be prototyped quickly in scheduler logic.
4. H05 because prefill IO is still dominant in prompt-heavy scenarios.
6. H13 is the next Stormrage-derived performance idea, but must stay opt-in and RDNA4-gated until MoE A/B proves a win.
7. H30 is completed and adjusted by G08 (2026-08-30): E070's `ne11<=1024` MMQ selector remains valid for the downloaded Q4_K_S small-batch path, but for Q4_K_M large-batch prefill the dequant+hipBLAS path is now faster than MMQ. Per-node trace shows Q4_K MMQ at 1 block/SM (LDS 88%, regs 236, occupancy 12.50%) while hipBLASLt is 2-4.7x faster on the same GEMM volumes; r3 neighbor A/B on Qwen3.8-27B Q4_K_M gives prompt `+3.29%` (byte-identical) and `+2.23%` on Qwen3.6-27B Q4_K_M. The `ggml_rdna4_q4k_mmq_max_ne11()` default is now `256` (env override preserved). Q5_K and the Q6_K MMQ-forcing negative remain unchanged.
8. G09 (2026-08-30) adds a second route-level win on the same lane: RDNA4 f32 cublas GEMMs (GDN projections + RoPE rotation K/V, 832 calls / ~628 ms) were running on the pure f32 path at ~0.4-0.85 TF/s. Sending them through f16 inputs with f32 accumulation in `runtime_compute.inc` gives prompt `+5.35%` (r3, byte-identical) and `+4.75%` on Qwen3.6-27B, with `GGML_ROCM_F32_GEMM_F16=0` as the verified rollback. The decode `mul_mat_vec_f_direct|f32` lane remains on the specialized f32 MMVF kernel.
9. G10 (2026-08-30) tested fusion of the remaining decode f32 MMVF pair (GDN `ssm_alpha`+`ssm_beta`, 5120->48, n=1): one two-output kernel is bit-exact but saves only ~9% of pair GPU time (0.0038 vs 0.0042 ms) because the shape is launch/latency-bound, and no wall gain was measured (decode ON 24.84 vs OFF 24.94). The lane is best treated as part of the launch-bound remainder of the decode graph, not as a kernel-resource problem; kept opt-in `GGML_ROCM_GDN_PAIR=1`.
10. G11 (2026-08-31) closes nearby Q5_K/Q6_K N=1 MMVQ body tweaks on the UD/f8 lane. Both kernels already have 100% occupancy and low registers. Q6 output head moves 994.63 MiB in 1.782 ms (`585.3 GB/s`, ~91.4% of physical bandwidth), so only fewer stored bytes can materially move it. Q5 pair streaming, `ds.y`, exact subgroup-sum sidecar, compact pair-dot, and Q6 packed-subtract variants were neutral/negative; the exact qsum route regressed weighted Q5 time 4.26%, while the compact pair lowered regs 42->39 but still regressed fused time 0.94%. All G11 runtime code was removed. The only plausible Q5 continuation is a byte-neutral load-time/prepacked scale/high-bit layout; do not repeat launch geometry, q8 reuse, sum caching, subtract or dot8 (int4-only) probes.
8. H31 is the active Vulkan Q3_K/FFN performance track again. D012 reached the old P002 `2 TPS` target with `2.0013 TPS` r3 using the opt-in `bn256 + lowtile3 + q3quad + GLU` stack on the 130k lane; D028 retargets the same lane to `2.4 TPS`. The new gate requires about `1.1992x` wall, `1277 tok/s` prompt if decode/overhead stay flat, `1.387x` local on dense FFN, or `1.260x` local on all-Q3. D029 rejects activation-only whole-FFN fusion and naive streaming because hidden materialization traffic is too small and streaming creates recompute/partial-output cliffs; whole-FFN is only worth reopening if it reduces Q3_K matmul work itself. Use D028/D029 plus `vulkan_q3k_prebuild_gate.py`, `vulkan_warptile_static_scout.py`, and `spirv_op_summary.py` before new probes; do not reopen ubatch, memory priority, output placement, m10240 q3quad inclusion, lowtile2/4, down split-K 6, MMVQ disable, no-graphics queue, vector-return q3quad, or adjacent-only FFN matching.
9. H32 was tested in E072 and rejected as a no-code-kept micro-optimization; Q3_K MMQ remains the dominant target, but this specific subtract rewrite is not useful.
10. H33 was tested in E073 and rejected as a no-code-kept packed32 load-side rewrite for active Q3_K MMQ.
11. H34 is kept as a 12k Q3 decode-heavy profile, not a ROCm prompt-heavy default. Post-driver E116 measured Vulkan decode at about `40 TPS`, E119 confirmed the backend on real-context 512-token session output, E120 refined the route to Vulkan q4 KV, E121 showed Vulkan q4 wins at 256 generated tokens, and E122 showed it still wins for repeated/session at 128 tokens while cold-first is tied/slightly ROCm. E124 rejects stacking `ngram-mod 12/16/32` on Vulkan (`0.7294x`, effective acceptance `0.004844`), so Vulkan route remains `spec=none`. E118 live-server sanity found normal thinking/answers with no `wm32-wn32`-style corruption. E117 rejects f16/q8 KV for the cold prompt-heavy ROCm default (`q4 11.9858`, f16 `11.9028`, q8 `11.6392`).
12. H35 is the ROCm Q3_K route track. E103 proved repeated staging, E106 refreshed the same-lane split, but E104 rejected persistent fp16 cache and E105 rejected existing-MMQ selector overrides. E191/E192 refreshed the current post-E190 bottleneck: `cublas_backend q3_K` dominates practical real-context `MUL_MAT forward`, and the Q3_K split is `5213.358 ms` with `src0_convert_ms=1637.070`, `src1_ms=364.309`, `gemm_ms=3203.883`. Continue only with a fused/direct Q3_K x F16 kernel design or graph-scheduling route that avoids persistent fp16 residency; do not repeat rejected route toggles (`GGML_CUDA_FORCE_MMQ_RUNTIME`, compute16, hipBLASLt, half2 store, dequant128, Q3_K persistent fp16 cache, existing-MMQ selector override, ubatch sweep). E106/E192 also add a workflow correction: split buckets need pre-stage sync/event timing before pure local attribution, and conversion-only fixes cannot move GEMM-dominated FFN shapes enough alone.
13. H36 is confirmed as a practical repeated/steady route: keep server prompt cache/checkpoints enabled for GUI/agent sessions, but never compare it as a cold-first kernel speedup. After driver `32.0.31007.5012`, E113 supersedes E112's `24/48/64` ngram with ROCm-only `ngram-mod 12/16/32` (`17.8934 -> 19.5051 TPS`, after-first mean `23.9038 TPS`). E114 rejects going shorter to match `8`, and E124 rejects transferring `ngram-mod 12/16/32` to Vulkan q4 because coverage is too sparse.
14. H09 to avoid misleading speculative projections in low-coverage runs. E107 shows local acceptance without coverage is not enough: tested cold-first ngram variants had effective acceptance `0.000000` to `0.004908` and lost wall TPS.
15. H10 to explain cross-mode speculative regressions with measured overhead.
16. H01 as a low-risk extension of existing ngram flow.
17. H37 is now the CPU/Vulkan fallback track. E125 keeps `--no-mmap` as the practical `-ngl 0` route (`1.7703 -> 1.8815 TPS`, `+6.28%`) and rejects `--no-op-offload`, `--mlock`, f16 KV, and Vulkan ngram for this lane. E126/E127 also reject simple x86 Q3_K mask/shuffle and prefetch micro-probes as promotion candidates. The next useful code route is Q3_K CPU matvec/repack work or deliberate partial offload when VRAM is available.
18. H38 is the current Vulkan 64k lane. E128 keeps `GGML_VK_ALLOW_GRAPHICS_QUEUE=1` + `--no-mmap` + `b8192/ub1024` as the best safe Vulkan 64k profile (`1.2896 -> 1.3406 TPS`, `+3.95%`) but confirms ROCm still wins the full 64k wall (`1.5545 TPS`) because prompt eval is faster. E129 rejects a simple FA coopmat1 tile sweep (`Bc32` and `Bc128`), E130 rejects `ub=768/1280`, E131 rejects FA mask-opt disable plus forced f16acc while keeping `GGML_VK_FA_ROUTE_TRACE=1`, and E132 rejects forced SHMEM staging because it falls back to scalar FA. E133 adds shape-level timing: the next Q3_K candidate must move `m=17408,n=1024,k=5120` or `m=5120,n=1024,k=17408`, while the next FA candidate must improve long-KV tail chunks without leaving coopmat1. E134 changes the search rule: single micro-branches are now below the gap. E135 proves `GGML_VK_FFN_ROUTE_TRACE=1` can see the dense FFN gate/up graph branch on the real 64k server lane. E136 shows dual-A/same-B FFN fusion without A-side work reduction projects only `1.4466 TPS`, below the `1.5545` ROCm target. E137 rejects current dual-N/same-A Q3_K in `mul_mm.comp`: it compiled with no scratch but raised VGPR to `120`, regressed pp7488, and exposed that shared-source B guards can hurt the default shader. E138 rejects forced FA split-k (`666.87 -> 96.29 tok/s`) because the existing route inserts temp writes, sync, and reduce dispatch per FA node. E139 rejects forcing Q3_K through the existing per-node predequant fallback (`969.61 -> 743.65` all-large pp7488; `929.40` even for only `k>=17000`) because the `~170 MiB` fp16 temp plus sync/global traffic dominates. E140 rejects existing matmul split-K for the reverse hot shape (`968.74 -> 966.21/964.46`) because it adds partial-output traffic/reduce without fixing a real parallelism shortage. E141 rejects changing the H38 KV dtype route: f16/f16 KV improves pp7488 only `970.03 -> 996.00` and fails real 64k fit, while q8_0/q8_0 regresses to `940.03`. E142 rejects larger query-row FA cm1 (`Br32/Bc32`) because it stays single-dispatch coopmat1 but raises VGPR to `133-134` and regresses pp. E143 rejects large-N Q3_K warptiles: `bn192-wn96` drops `974.19 -> 760.78`, `bn256-*` drops to about `660`, and `bn192-wm128-wn96` spills scratch and collapses to `137.71`, proving that current larger-tile A-reuse loses to VGPR/LDS/occupancy. E144 rejects `BK16`: it lowers resources to `70 VGPR / 12288 B LDS`, but drops pp7488 `972.77 -> 587.52` because doubled K-loop/barrier cadence dominates. E145 rejects FA `D_split=4/16`: the route stays q4/q4 coopmat1 with the same resource report, but pp7488 drops `978.88 -> 953.24/951.54`. E146 rejects BM256: it lowers VGPR to `94` and halves B/workgroup proxy, but LDS rises to `31744 B` and pp7488 drops `972.84 -> 916.62`. E147 rejects broad persistent fp16/int8 Q3_K layout on memory (`+25.03 GiB` / `+9.09 GiB` for FFN) and downgrades signed-nibble layout to instruction-gated, low-confidence work. Next work must be a route-level Q3_K/FA stack, with Q3_K paused until a topology removes A-pair count without fp16 temp/sync/reduce, accumulator blowup, near-limit LDS, or extra barrier/reduce cadence; the fresher complex route is now q4 FA long-KV shader-body work. Do not spend this lane on ngram/spec, trivial FA `Bc` retuning, mask/f16acc/staging toggles, FA split-k forcing, f16/q8 KV pivots, larger-Br cm1, FA `D_split` retunes, launch-only GLU fusion, current dual-N accumulation, existing predequant fallback, existing matmul split-K, larger-N/M warptiles, BK shrink, broad Q3_K fp16/int8 layout, or nearby ubatch sweeps.
19. H40 is the DFlash preparation track. Use `docs/research/DFLASH_IMPLEMENTATION_PREP.md` as the staged port checklist while active ROCm tuning is running in parallel.
20. H41 is the first new route-level follow-up for ROCm Q3_K: measure temporal locality of repeated `src0` staging before attempting any transient reuse implementation.
21. H42 is the strongest code route after H41 if reuse locality is weak or only partial. For the P002 130k ROCm lane, D002 refreshes this as a Q3_K MMQ body/dataflow problem rather than the old cublas split path: Q3_K MMQ is `~47%` of diagnostic wall and S002A rejects a padded 32-bit-load-only rewrite as too small/noisy. The next low-level branch must change the MMQ body or dataflow enough to approach about `2.04x` local at that measured `~0.47` share, or broaden the touched route toward `~0.8` all-Q3/FFN share where `~1.43x` local can reach 2 TPS; do not merely swap aligned loads or selector knobs.
22. H43 is the heavy structural route and must stay correctness-first: padded Q3_K backend storage, not storage-blind helper tweaks.

## Evidence Snapshot (E006 Retest)

- Supported by measured evidence: H11 as the allocator/residency root cause for the native `ub904/1024` cliff, H08 as a boundary/cliff symptom class, H09.
- Supported as modeling-next-step: H10.
- Analytic-only so far: H02.
- Plausible but not measured yet: H01, H03, H04, H05, H06, H07, H13.
- Active Vulkan Q3_K track: H31 / E082 + E086 accepted, E102 defaulted the fast AMD large-matmul path for the local AMD proprietary coopmat device. Current no-env pp7488 is `983.48` on `matmul_q3_k_f32_f16acc_aligned_l`; disabling auto-large drops to `708.19`, proving the rollback gate. E100 fresh 32k controls show the broader gap is now smaller in aggregate but still prompt/prefill-side (`993.94` Vulkan vs `1132.44` ROCm prompt eval), while Vulkan decode is faster (`32.93` vs `28.49`). E257 refocused on dense Qwen3.6-27B Vulkan 12k and confirmed the current cold profile as `ctx=12288,b=7168,ub=1024,q4_0/spec=none`: `7.0319 TPS` r3 and prompt `999.22 tok/s`, `+5.12%` wall vs the previous `b6144/ub2048` Vulkan control. The E257 trace attributes `82.71%` of parsed time to Q3_K `MUL_MAT` and only `9.60%` to FlashAttention. E258 then tested a backend-private Q3_K transpose-A storage route; it activated the dedicated `matmul_q3_k_f32_transa_f16acc_aligned_l` path and raised prompt eval slightly, but wall regressed `6.9827 -> 6.9053 TPS` because decode fell `40.09 -> 35.76 tok/s`, so the source prototype was removed. E259 closes nearby no-code gates on the same lane: `batch=7680` regressed, and f16/f16 KV confirmed only `7.0543 TPS` r3 (`+0.32%` vs E257, decode lower), so the GUI default remains q4/q4. E260 closes older transfer gates on the exact E257 shape: graphics queue `6.8663 TPS`, graphics queue + `--no-mmap` `6.8743 TPS`, `batch=8192` `6.7312 TPS`, and broad f16-disable route `5.2700 TPS`, all prompt-side regressions. E264 rejects graph-level F16 `src1` casts for FFN: gate/up dropped `7.1320 -> 5.9323 TPS`, and down-only dropped to `6.8000 TPS`, so any future F16 activation route must avoid per-layer cast temp traffic. The next source route still needs a genuine Q3_K topology rather than another FA flag, KV dtype, batch tweak, graphics queue/mmap/f16 transfer, activation cast, transpose-A storage flip, or helper/tile retune. E095 shows AMD RX 9070 XT runtime has KHR coopmat but not NV coopmat2, so `mul_mm_cm2` is not an active AMD route on this driver. E096 adds SPIR-V opcode fingerprints for generated Q3_K shaders; E097 corrects the warptile static scout to subgroup `64`, coopmat `16x16x16`, and base Q3 LDS `20480 B`. Prebuild screening is required before more H31 probes because many nearby ideas are already measured negative or invalid: `LOAD_VEC_A=8`, pair-scale/helper-only dequant reuse, packed32 pair helper, stride16/19/20/22, f16 dequant, unsigned scale, `wn48/wn96`, `bm256`/`bn256*`, Q8_1/int-dot, aligned-store cleanup, Q3_K shift/mask arithmetic, Q3_K scale-int arithmetic, BK-depth tweaks without resource proof, and old invalid `wm32-wn32`.
- Active ROCm new-route track: H35 starts from the E049/E054 finding that the large Q3_K fp16 hipBLAS path is conversion-dominated for hot prompt shapes: Q3_K `src0_convert_ms=3370.32 ms`, target `6144x5120@ncols2048` convert `1430.88 ms`, allocation effectively zero. E103 proved repeated Q3_K staging (`2792` rows, `349` keys, all repeated `8` times), but the working set is too wide for a small cache and full fp16 cache is too large (`42.002 GiB`). E104 rejected persistent `attn_gate` cache (`11.74 -> 9.56 TPS`, 480 MiB variant `11.59 TPS`) despite real conversion reduction, E105 rejected existing-MMQ route overrides (`11.74 -> 11.54/11.68/11.44 TPS`), and E123 rejected allocator chunk-size retuning after the driver update (`128 MiB`, `64 MiB`, single-chunk all below E113). E106 refreshed the same-lane control at `11.8464 TPS` and split trace (`2792` Q3_K rows, `src0_convert_ms=3257.251`, `gemm_ms=6107.363`) but also showed a tracing caveat: without pre-stage sync/event timing, split-stage ms may include earlier queued GPU work. Route toggles and simple conversion kernels are already rejected (`GGML_CUDA_FORCE_MMQ_RUNTIME`, compute16, hipBLASLt, dequant128, half2 store, persistent fp16 cache, existing-MMQ selector override, vbuffer chunk-size retuning). Only a shape-specific fused Q3_K x F16 kernel still has enough structural ceiling.
- Confirmed repeated/steady route: H36 / E111 keeps prompt cache/checkpoints as a practical session route. Same `ctx=12288,b=6144,ub=2048,q4_0/spec=none` workload measured `14.6132 TPS` r1 and `17.7984 TPS` r3 with reuse enabled; after the first task, repeated tasks ran about `20.00 TPS` because checkpoints restored the shared `5370`-token prefix. E112 stacked `ngram-mod 24/48/64` on that route and measured `18.7194 TPS`, but post-driver E113 retuned the stack: `ngram-mod 12/16/32` measured `19.0148` and `19.5051 TPS`, with after-first mean `23.1681-23.9038 TPS` and effective acceptance `0.035028`. This is a real GUI/agent-session gain, not a cold-first kernel/default claim.
- Active CPU/Vulkan fallback route: H37 / E125 maps `build-vulkan -ngl 0` with Qwen3.6-27B-Q3_K_S. Default op-offloaded prompt route measured `1.7703 TPS`; `--no-mmap` measured `1.8815 TPS`; `--no-mmap --threads 6 --threads-batch 6` measured `1.8931 TPS` but mostly shifts prompt/decode tradeoff. `--no-op-offload` collapsed to `0.89 TPS`, proving that `-ngl 0` should keep Vulkan op-offload unless the goal is a pure-CPU negative control. Partial offload scales predictably (`ngl32` `3.46 TPS`, `ngl48` `6.03 TPS`, full `ngl65` `28.93 TPS`) because the remaining CPU Q3_K layers dominate.
- Active Vulkan 64k route: H38 / E128 maps `ctx=65536` with q4/q4 KV and real server repo-snapshot prompt (`57409-57575` prompt tokens, depending on repo snapshot). Vulkan baseline measured `1.2896 TPS`, best no-code profile measured `1.3406 TPS`, and ROCm comparison measured `1.5545 TPS`. Vulkan decode remains faster (`36.58` vs `22.83 tok/s`), but full-context wall loses on prefill. Perf trace attributes `47.79%` to `MUL_MAT q3_K` and `38.03%` to `FLASH_ATTN_EXT`. E129 rejects FA coopmat1 `Bc=32/128` retuning and E130 rejects `ub=768/1280`. E131 adds `GGML_VK_FA_ROUTE_TRACE=1` and maps the active FA route as aligned coopmat1 q4/q4 with `Br=16,Bc=64,D_split=8,row_split=4`, main `N=1024`, growing `KV`, `split_k=1`, and `use_mask_opt=1`; it rejects mask-opt disable and forced f16acc. E132 records FA resources (`98 VGPR`, `76 SGPR`, `26112 B LDS`, `0 scratch`) and rejects SHMEM staging because it falls back to scalar FA. E133 adds shape-level timing: top Q3_K forms are `m=17408,n=1024,k=5120` (`20338.69 ms`) and `m=5120,n=1024,k=17408` (`11289.87 ms`), together about `74.1%` of parsed Q3_K time. E134 adds the route-ceiling gate: FFN gate/up alone cannot close the lane unless it is implausibly `2.234x` faster locally; the realistic target is either `1.357x` on all Q3_K, `1.494x` on FA, or about `1.172x` across Q3_K+FA together. E135 proves the dense FFN gate/up branch is visible to Vulkan graph code on the real 64k server lane (`63 x q3_K SWIGLU`, `m=17408,n=1024,k=5120`). E139 proves the existing Q3_K predequant fallback is not that branch: it loses despite lower VGPR because it serializes through fp16 temp traffic. E140 proves existing matmul split-K is also not the missing branch for `k=17408`: the shape already has enough workgroups. E145 proves simple FA `D_split` retuning is not the missing branch either: `D_split=4/16` keeps resources unchanged and regresses. E146 proves simple larger-M Q3_K tiling is not the missing branch: BM256 lowers VGPR and B/workgroup proxy but loses to near-limit LDS. E147 proves broad persistent fp16/int8 Q3_K layouts are not viable on memory, while signed-nibble layout is only a low-confidence instruction-gated branch. The next useful code target is FA shader-body/per-KV-tail work or a genuinely new Q3_K topology, not another nearby tile, flag, fallback staging route, split/reduce topology, cm1 split flip, larger current-tile variant, or broad Q3_K fp16/int8 layout.
- Rejected H37 CPU micro-probes: E126 clean-vs-dirty Q3_K shuffle/mask preload changed aggregate from `1.8067` to `1.8611` but did not improve decode (`2.4833 -> 2.4800 tok/s`), so it is prompt variance, not a proven decode win. E127 Q3_K next-block prefetch was only `+1.13%` r1 (`2.0716 -> 2.0950 TPS`) and was reverted. Do not spend more cycles on isolated mask/prefetch tweaks without a stronger microbench; go to Q3_K repack/interleaved matvec.
- Rejected narrow probe: H32 / E072, Q3_K MMQ offset subtract micro-optimization based on the existing MMVQ bit-twiddle; first active gate was a small regression, so the shader patch was reverted.
- Rejected narrow probe: H33 / E073, Q3_K MMQ packed32 load-side rewrite; first active gate was a small regression, so the shader patch was reverted.
- Kept profile change: H34 / E116+E118+E119+E120+E121+E122, Q3_K_S ctx=12288 Vulkan q4 decode-heavy route; r3 decode gate was about `40 TPS`, real-context 512-token session r3 reached `32.1668 TPS`, 256-token session r3 beat ROCm by `+19.00%`, and 128-token repeated/session still won by `+7.02%` aggregate while cold-only tied. Live-server output sanity was accepted by the user. E117 keeps ROCm prompt-heavy default on q4 KV.
