# RPC Changes Log (rpc-vulkan)

Хронология изменений RPC-контура ветки `rpc-vulkan` (клиент AMD RX 9070 XT ×2
+ сервер RTX 3080 по LAN, 1 GbE). Детальные протоколы/эксперименты — в
`RPC_PREFILL_RESUME_PLAYBOOK.md`, итоговые числа — в
`build_logs/agent-workload/BENCH_RECENT.md`.

Формат: дата — что изменилось — результат (на 2026-08-26).

## 2026-08-22 — восстановление RPC backend для Vulkan offload

- Ветка `rpc-vulkan` создана от `master` (в master RPC удалён).
- Фикс краша сервера `SIGSEGV 139` при загрузке модели: буферную эвристику
  `ggml_backend_rpc_buffer_type_get_alloc_size` заменено на запрос к серверу
  для квантованных тензоров (`ggml-rpc.cpp` ~стр. 610): Vulkan-сервер пишет
  q3_K/q6_K в device-раскладке (+2 байта/блок), host-размер выходил за
  пределы буфера на ~9.4 МБ. Коммит `eb0d3c5ec`.
- Третий рычаг: сдвиг слоёв с дисплейной VK0 на сервер + `SO_SNDBUF 16MB`
  на клиентском сокете (снял блок 88 мс/убатч на дренаже l_out-43).

## 2026-08-23 — 12K цель достигнута

- `alloc_size cache` (fnv по endpoint/type/ne/nb/op/params): alloc 537 → 1.3 мс/убатч
  (srcs в ключ нельзя — у FA-нод kv-вьюхи растут с n_past).
- F16-стык активаций (`GGML_RPC_TENSOR_FLAG_ACT_F16`, `l_out-*`/`result_output`):
  +20% prefill (1969 → 2362 ptps на Qwen3.5-9B 16K), decode +8.5%.
- Маска-оптимизация отключена по умолчанию (`GGML_RPC_ENABLE_MASK_NULL=1`
  включает): NULL-подстановка давала ppl 2.09 — модель «видит будущее»
  (Vulkan-FA с MASK_ENABLE=false = полное внимание). Цена: +27% времени
  PPL-прохода.
- MTP-RPC починен: `RPC_CMD_GRAPH_RECOMPUTE` + структурный hash графа,
  сервер хранит `stored_graphs` по hash (лимит 16); acceptance 1.46% → 59.7%.
- Результат 27B 12K (коммит `62da5f1ab`): 839 → **1314 ptps / 23.6 t/s**
  (цель ≥1277 = 80% соло; PPL 4.0148 = эталон). Конфиг: `-ts 0.9,0.6,1.5`.

## 2026-08-25 — клиент — критический путь; лучи диагноза

- `GGML_RPC_ASYNC_GRAPH=1`: серверный worker + async `GRAPH_COMPUTE_ASYNC`
  (клиент fire-and-forget); +8.3% на 14K при отказе от блокирующего
  RECOMPUTE (1384.98 vs 1278.55 ctl, тот же `-ts 1,0.8,1.2`).
- Без RECOMPUTE (полный async) — A/B: 0.2% (шум), т.к. клиент — критический
  путь (735 мс/уб; сервер 576 мс/уб уже перекрыт).
- Split-timing: узкое место prefill — копии `l_out` (83% стены:
  VK1→VK0 7600 мс + VK0→RPC0 6924 мс из 17.5 с @14K/21 убатч).
- 94K `-ts 1,1,1.1`: 1066.81 t/s (плато, -ts 1.2 = alloc fail на 3080),
  local 98K = 1355.34 → gap −21%.

## 2026-08-26 — рефактор: async-копии; фикс дедлока; 94K +4.3%

- Рефактор-проба «полного async» (снятие входных барьеров + worker-копии
  GPU→RPC в per-socket очереди) → **детерминированный дедлок** на 3-4-м
  декодном убатче. Причина: `ggml_vk_synchronize`
  (`ggml-vulkan/runtime/vk_backend_execution.inc:2`) не потокобезопасна —
  общий `compute_ctx`/`fence`/`submit_pending`; RPC-worker вызывал её
  параллельно с `graph_compute` того же VK-бэкенда.
- ФИКС (код в коммите): **снэпшот в главном потоке планировщика**
  (`ggml_backend_synchronize` + `ggml_backend_tensor_get` в main), per-socket
  worker только шлёт захваченный payload; в работу добавлен `rpc_send_tensor_data`
  (сериализованные metadata захватываются на submit, worker не трогает живые
  ggml-объекты).
- Сняты входные барьеры RPC-сплита: `split4 copy 288 → 0.16 мс`;
  INPUT-копии (leaf_56 и др.) идут async с host-снэпшотом.
- Результаты: 14K 1363.10/27.68 (паритет, шум ±4%), 94K **1116.78**/19.52
  = +4.3% vs 1070.72 (промпт идентичен 58 185 ток).
- Диагностика rf6: `l_out-39` 20 МБ = sync VK0 241-266 мс + get 4.9-6.7 мс —
  остаток = ожидание самого VK0-графа; RPC-путь больше не лимит на 14K.

## Следующая стена (работа в процессе)

- Локальная копия VK1→VK0 (`l_out-21` 20 МБ, 268 мс): путь через host
  staging (`ggml_vk_buffer_copy` MULTI_DEVICE, `vk_transfer.inc:1033`) —
  GPU→host `waitForFences` + host→GPU ≈ 74 МБ/с вместо ожидаемых сотен МБ/с.
- Кандидаты: асинхронный transfer с double-buffer, P2P (external memory,
  крупно), либо перепланировка границы слоёв, чтобы не переносить 20 МБ.

### 2026-08-26 (позднее) — гипотеза «копия = стена» ОПРОВЕРГНУТА

- Тайминг-патч фаз MULTI_DEVICE копии (`GGML_VK_TRACE=1` в
  `vk_transfer.inc`): 94K 20 МБ = 1.7 мс read + 1.9 мс write = **3.6 мс**
  (22-27 МБ = 4.5-8.9 мс). Копия больше НЕ помеха: 33 МБ/с → ~5.8 ГБ/с
  после snapshot-рефактора.
- A/B подтверждение на 94K (rf11, тот же контракт): **1110.87 ptps /
  19.24 decode** vs rf9 1116.78/19.52 — паритет (−0.5%), воспроизводимость
  +4.3% к базе подтверждена повторным прогоном.
- Следующая стена: НЕ копия, а **последовательные GPU-графы одного убатча**
  (VK1 62 мс → VK0 160 мс → RPC0-сервер ~576 мс/уб на 94K) — межубatch
  конвейер (VK1(N+1) ∥ VK0(N) ∥ RPC0(N-1)) требует 2-буферной схемы в
  `llama-context`/scheduler; потенциал частично скрыть серверную задержку.

### 2026-08-26 (вечер) — парный A/B на 49K (rf12/rf13)

- Тот же контракт, `-ts 1,1,1.1`, MTP n=4, q8_0 KV, 30 840 токенов:
  - **рефактор rf12**: 1264.81 ptps / 23.96 decode (31.04с + 28.74с),
  - **стабильный `20e2630a6` rf13**: 1230.47 ptps / 24.30 decode
    (31.57с + 29.37с),
  - **Δ = +2.8% prompt** (в ту же сторону, что +4.3% на 94K; decode −1.4% —
    шум).
- Для сравнения: 2026-08-23 тот же 49K-путь (spec=none, старый баланс
  `-ts 28,36,36`) = 770.47 ptps — сейчас с MTP и новым балансом 1264.8.
- Вывод: рефактор подтверждён на двух лейнах (49K +2.8%, 94K +4.3%);
  14K — паритет (стены RPC там уже нет).

### 2026-08-26 (поздний вечер) — async-get logits; точная стена 94K

- **Добавлен `ggml_backend_rpc_get_async`** (ggml-rpc.cpp): GET_TENSOR
  выполняется per-socket worker-ом в упорядоченной очереди, данные пишутся
  в host-буфер вызывающего; барьер — существующий `rpc_wait_pending_copies`
  (вызывается из `ggml_backend_rpc_synchronize` → `llama_synchronize` перед
  сэмплированием). Раньше `get_tensor_async==NULL` заставлял main делать
  полный барьер RPC-очереди + синхронный GET на каждый убатч.
  Прототип ограничен `result_output` (t_logits): их буфер читается только
  после `llama_synchronize`; embd/NextN оставлены синхронными.
- 14K (rf15): **1387.77 ptps / 28.03, acc 74.8%** — лучший замер лейна
  (rf7: 1363.10/27.68); +1.8%, без падений и регрессий качества.
- 94K (rf16): 1104.22/19.20, acc 43.2% — паритет с rf9/rf11 (см. ниже);
  на 94K клиентская блокировка не была лимитером.
- Точная стена 94K (rf14 split-timing, prefill ubatch 1024):
  - VK1-граф ≈ 256 мс (ожидание в split3 `l_out-21` 20 МБ — это
    ВЫПОЛНЕНИЕ VK1-графа, не копия: VK→VK cross-device
    `cpy_tensor_async` откатывается на sync);
  - VK0-граф ≈ 276 мс (split4 `l_out-42`);
  - локально: 532 мс/уб; суммарный wall ≈ 638+ мс/уб;
  - остальное — серверная 3080 (RPC-канал, на 94K ~576→900 мс/уб с
    ростом KV) — главный лимитер поздних убатчей;
  - выигрыш возможен только от балансировки слоёв сервер→локальные
    (VRAM-лимит 3080 = −ts 1.1) или от расщепления серверной очереди.
- rf17-94k (`-ts 1,1.1,0.9` — +1 слой на VK0, −2 на сервере):
  1024.00/24.85 vs rf9 1116.78/19.52 → **−8.3% prefill** (decode +27%).
  Вывод: `-ts 1,1,1.1` близок к оптимуму 94K; локальная цепочка
  VK1→VK0 уже лимитирует, сдвиг слоёв на локальные не окупается.
- rf18-94k (`GGML_RPC_ACT_F8=1` — F8-сжатие промежуточных `l_out-*`):
  1093.41/16.36 vs rf9 1116.78/19.52 → **−2.1% prefill, −16% decode**.
  F8-активации отвергнуты: передача не была лимитером (async-get уже
  скрыл её), а потеря точности видна в МТП-декоде. `GGML_RPC_ACT_F8`
  остаётся opt-in, default F16.
- rf19-49k (async-get, тот же 30 840-токенный промпт): 1201.7/23.61,
  acc 58.6% (agg 1251.4/25.2) — паритет с rf12 (1264.8)/rf13 (1230.5):
  на 49K клиентское ожидание уже не лимитер.
- rf20/rf21-14k (`--ubatch-size 2048`): 1242.3/1205.8 — НЕСРАВНИМО
  с rf15 (1387.8): снапшот дал 8809 ток вместо 7977 (изменения доков в
  репо меняют repo-snapshot). ВАЖНО: с 2026-08-26 14K-лейн требует
  фиксации содержимого снапшота для A/B; ub2048 на одном количестве
  токенов не подтверждён и по профилю (FA-стоимость ∝ batch×KV)
  улучшения не обещает.
- Итог ветки rf9-rf21: единственный подтверждённый прирост — async-get
  на 14K (+1.8%, рекорд лейна 1387.77). Все остальные рычаги
  (ts-баланс, F8-активации, async-get на 49K/94K, ubatch 2048) —
  паритет или хуже. Следующий шаг — только структурный
  (run-ahead/конвейер серверной очереди), требует кода в llama-context.
- ЛОКАЛЬНЫЕ КОНТРОЛИ (регрессия планировщика, 2026-08-26):
  - rf22-local-94k (MTP n4, без RPC): 1391.96/55.40, acc 90.8% —
    быстрее любого RPC-лейна (они 1104-1117); локальный путь цел.
  - rf23-local-94k (spec=none, без RPC): 1335.21/24.70 — против
    канона README.md:162 q38-rb-vk-98k-q8-none-r2 = 1355.34/24.55
    (2026-08-14): −1.5% prompt, +0.6% decode — в пределах шума 94K.
  - Причина проверки: рефактор async-копий в ggml-backend.cpp
    (строки 1705/1725/1742/1837) снимает ожидания не только для RPC:
    `cpy_tensor_async != NULL` ИСТИННО и для локальных VK-бэкендов
    (vk_backend_buffers.inc:564). Вывод: локальная скорость не
    пострадала; vk_transfer/vk_common менялись только под
    GGML_VK_TRACE (измерительные, не поведение).

### 2026-08-26 — рефакторинг: RPC вынесен в отдельный стек файлов

- `ggml-rpc.cpp` (~3300 строк) разделён на слои (без изменения
  семантики, только по границам функций; генераторы:
  `scripts/research/rpc_split.py` + `rpc_split_post.py`):
  - `rpc_types.h` — протокол (структуры/команды/флаги);
  - `rpc_internal.h` — общие типы/декларации/env-гейты;
  - `rpc_common.cpp` — общий транспорт+референсные функции
    (send/recv, get_socket, очередь, hashes);
  - `rpc_client.cpp` — клиентский ggml_backend;
  - `rpc_server.cpp` — сервер + реестр;
  - `transport.cpp/h` (существовал) — сокетный слой.
- Сборка clean; функциональная проверка:
  - rf24 RPC-14K (async-get, MTP n4, ts 1,1,1.1): 1328.65/25.12 —
    полностью рабочий RPC-контур после раскладки;
  - rf25 local-94K spec=none: 1351.18/25.05 — канон README
    1355.34/24.55 (−0.3%, шум) — локальный путь не задет.
- Полный разбор архитектуры/отличий от локальной работы и карта
  узких мест: новый `docs/research/rpc-vulkan/RPC_ARCHITECTURE.md`.

### 2026-08-26 (вечер) — no-flush KV (отрицательный результат, паритет)

- Новый `RPC_CMD_SET_TENSOR_NOFLUSH`: клиент шлёт KV-входы
  (`attn_inp_k_rot`/`attn_inp_v_rot` — область текущего убатча, каузальное
  чтение никогда не смотрит вперёд) без `graph_compute_wait()` на сервере.
  Гипотеза: drain на SET KV N+1, ожидающий серверный график N, и есть
  сериализация убатчей.
- rf26-14K: 1351.29/27.41 (+1.7% к rf24; качество ок).
- rf27-94K: **1113.34/19.41, acc 43.2%** — паритет с rf9 (1116.78/19.52):
  гипотеза НЕ подтвердилась (KV-передачи не были дросселем).
- Наблюдение rf14: wall 94K ≈ 917 мс/убатч при том, что локальные графы
  клиента ~41 мс работы + ожидание: **wall целиком определяется сервером**.
### 2026-08-26 (поздний вечер) — 94K timeline: стена = GPU-compute, не очередь

- Профиль trace-94k-rpc-deep (GGML_RPC_TIMELINE, 26 убатчей RPC0-графов
  за prefill, KV 8K → 92K):
  - все GRAPH_COMPUTE_ASYNC submit-ятся с flush ≈ 0.25 мс — сервер
    принимает графы мгновенно, «очередь команд» не блокирует;
  - единственный длинный drain — SET l_out (10.5 МБ) на следующем убатче:
    flush 657→835 мс = остаток compute предыдущего серверного графа;
  - период убатча растёт с KV: 666 мс при KV≈70K → 838 мс при KV≈90K.
- A/B баланса -ts: 1.1 → 1113.3 (rf27), 1.0 → 1054, 0.8 → 977; 1.5 —
  alloc fail на 3080 (26 слоёв + KV не влезают в 10 ГБ). Меньше слоёв на
  3080 ХУЖЕ: KV уходит на дисплейную VK0 → её prefill-время растёт.
- ВЫВОД: «один async-worker / последовательные графы» — НЕ главная
  стена. Wall = суммарное GPU-compute (FATTN/MM при больших KV) на
  границе устройств + локальная часть (которая доминирует при малых
  серверных слоях). Двухбуферный серверный конвейер скроет только
  локальные ~41-50 мс/уб (+5-6%), а не 18%.
- Следующие кандидаты (требуют отдельного решения):
  (a) ускорение серверных NV кернелов (VVK FATTN на 3080, KV-путь);
  (b) распределение KV/слоёв (sm layer, -devd, KV-сплиты);
  (c) серверные слои в F16-буфере с повторным использованием.
### 2026-08-27 — весь KV на 3080 через LLAMA_KV_DEVICE: катастрофа

- `LLAMA_KV_DEVICE=<dev>` (llama-kv-cache.cpp:112-127, имя RPC-устройства
  из llama_prepare_model_devices — просто "RPC0", НЕ "RPC0[ep]") форсит
  все KV-слои на один бэкенд при layer-split.
- rf29 (имя с endpoint): нет в списке → layer-local; rf30
  (LLAMA_KV_DEVICE=RPC0, 14K, иначе тот же контракт): **274-280 ptps,
  decode 1.1-1.8 t/s** против rf26 1351.3/27.4 = **в 4.8-5 раз хуже**
  (кросс-устройственные KV-копии на каждый убатч; acc 45.5/92.5%).
- Вывод: KV нельзя выносить отдельно от слоёв; скорость растёт только
  через перенос слоя+KV вместе (layer-split) — см. rf27 vs rf28.
- Для целей «снять VRAM-давление локальных при огромном ctx» рабочие
  инструменты: -ts 1,1,1.1 (22/62 слоя на 3080), KV q4_0 (для
  локальной доли), vk_host_kv_layers (host-KV, форк).

### 2026-08-27 — топология RPC0-первая + балансировка (rf31-rf39)

- Идея: поставить RPC0 ПЕРВЫМ устройством: `-dev RPC0,Vulkan0,Vulkan1`:
  первые слои на 3080, середина на VK0, хвост (последние слои +
  output.weight + F16-KV хвост MTP) на основной VK1 → финальный результат
  локально, возврат по сети не нужен на каждый токен.
- Результаты (14K, MTP n=4, q8_0 KV, свежая сессия):
  rf31 (RPC0,VK1,VK0; 1.1,1,1) 974.33/39.85 (хвост на дисплейной VK0)
  rf32 (RPC0,VK0,VK1; 1.1,1,1) 1003.91/40.13
  rf34 (0.9,1,1.3; 18/20/26) 1126.81/47.97  <- БАЗА
  rf35 (0.8,1,1.4; 16/20/28) 1168.03/45.09
  rf36 (0.7,1,1.5; 14/20/30) 1204.95/43.03
  rf38 (0.9,1.15,1.15; 18/23/23 - выравнивание 9070) 1105.31/41.16
  rf39 (0.9,1.1,1.2; 18/22/24) 1103.88/40.96
- 94K: rf33 (1.1,1,1) 819.74/29.96; rf37 (0.9,1,1.3) 980.76/32.78
  (rf27 старый порядок: 1113.34/19.41).
- ВЫВОДЫ:
  1) decode растёт на +45-75% (27.4→48.0 на 14K; 19.4→32.8 на 94K) —
     это «рабочая скорость», соответствует приоритету интерактив/GUI;
  2) prefill проседает (−12% на 94K) из-за сетевого возврата сегмента
     10.5 МБ (~88 мс/уб по 1GbE) в критическом пути каждого убатча;
  3) выравнивание 9070 НЕ помогает: дисплейная VK0 не тянет >20 слоёв
     (23/23 и 22/24 хуже 20/26); хвост должен быть на не-дисплейной VK1;
  4) снижение слоёв на 3080 (0.9→0.8→0.7) поднимает prefill (+4%/шаг),
     но валит decode (−4-6%/шаг); rf34 - компромисс пользователя.
- БАЗОВАЯ РАБОЧАЯ КОНФИГУРАЦИЯ (2026-08-27, rf34/rf37):
  `--rpc 192.168.1.60:50052 -fit off --cache-ram 0 --ctx-checkpoints 0
   -dev RPC0,Vulkan0,Vulkan1 -sm layer -ts 0.9,1,1.3
   --spec-type draft-mtp --spec-draft-n-max 4`
  (18/20/26 слоёв; 94K: 980.76/32.78, 14K: 1126.81/47.97).
- Следующая задача: догнать prefill локальной связки (94K local
  1355.34/19.41) при сохранении топологии; кандидаты: F8-сжатие l_out
  (10.5→5.3 МБ, экономия ~44 мс/уб), -ts 0.8,1,1.4 на 94K, серверные
  NV-кернелы. Выигрыш при успехе: 3-GPU превосходит 2-GPU на огромных
  контекстах, где локальным не хватает VRAM (FP8).

### 2026-08-27 — prefill run-ahead:  стена = копия RPC0→VK0 (rf40-rf46)

- RPC0-first топология: split-структура префилл-графа (GGML_SCHED_SPLIT_TIMING):
  CPU(embed) → RPC0(слои 0-17, 1242 узла) → VK0(19-39, 1341) → VK1(40-63, 1553).
- СТЕНА: копия l_out-18 RPC0→VK0 в compute_splits: **221-238 мс/уб**
  (rf44: copy=237.8, compute=21.4; rf45) = синхронный wait серверного
  графа + get 10.5 МБ по 1GbE + set на VK0. Именно она, а не графы,
  даёт wall ≈ сумма (не max) локального+серверного времени.
- Попытки кода (14K/94K, база rf34/rf37):
  * LLAMA_RPC_RUN_AHEAD=1 (force fresh alloc графа каждый префилл-уб:
    cur_copy чередует 4 буфера планировщика, per-copy события снимают
    глобальную sched_synchronize): 14K 1167.1/40.2 (+3.6%), 94K
    984.63/33.7 (+0.4%) - NEGLIGIBLE на 94K: копия остаётся в
    критическом пути, следующий RPC-граф submit-ится после неё.
  * GGML_RPC_ACT_F8=1 (l_out 10.5→5.3 МБ): 94K 961.8/31.8 (-2%) -
    конверсии съедают выигрыш.
  * Маск-пин на RPC0 (единый RPC-граф вместо 2 сплитов): 14K 731/29 -
    ХУЖЕ (МТП/декод деградирует), ОТКАЧЕНО.
  * GGML_SCHED_ASYNC_SPLIT_COPY=1 + run-ahead: 14K 958.4/35.8 - хуже
    (background VK0-set конкурирует), НЕ включать.
- ВЫВОД: для 94K-tapget нужен НЕ run-ahead, а рефакторинг межсегментной
  копии RPC→local (двухэтапная: async pull в host-worker → set в
  планировщике) - большая работа в ggml-backend, рискованная.
- Рекомендованный рабочий конфиг остаётся rf34/rf37:
  `-dev RPC0,Vulkan0,Vulkan1 -sm layer -ts 0.9,1,1.3` (decode +69%, prefill 981).

### 2026-08-27 — баланс 94K: rf47 (0.8,1,1.4) — новый рабочий конфиг

- 94K sweep (RPC0,Vulkan0,Vulkan1, MTP n=4, KV q8_0):
  rf37 (0.9,1,1.3; 18/20/26) 980.76/32.78
  rf47 (0.8,1,1.4; 16/20/28) **1029.64/39.36**  <- лучший компромисс
  rf49 (0.75,1,1.45; 17/20/27) 1041.79/31.52
  rf48 (0.7,1,1.5; 14/20/30) 1047.13/30.89
- Выбор rf47: prefill +5.0% к rf37 и decode +20% (39.36 против 32.78);
  rf48 ловит ещё +1.7% prefill, но теряет 21% decode.
- ФИНАЛЬНЫЙ РАБОЧИЙ КОНФИГ (2026-08-27):
  `--rpc 192.168.1.60:50052 -fit off --cache-ram 0 --ctx-checkpoints 0
   -dev RPC0,Vulkan0,Vulkan1 -sm layer -ts 0.8,1,1.4
   --spec-type draft-mtp --spec-draft-n-max 4`
  94K: 1029.64/39.36 (local 2-GPU: 1355.34/19.41: prefill-гэп −24%,
  decode +103%).
- Цель (prefill == local) остаётся открытой: нужен двухэтапный async
  pull копии l_out-18 RPC0→VK0 (~221 мс/уб) в ggml-backend или 10GbE.
