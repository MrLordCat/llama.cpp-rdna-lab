# Generated operation tables

Place local `test-backend-ops support --output csv` results in this directory,
then run `scripts/create_ops_docs.py` to regenerate `docs/ops.md`.

The CSV inputs and generated table are intentionally ignored. They describe a
particular build and become stale when backend options or drivers change.
