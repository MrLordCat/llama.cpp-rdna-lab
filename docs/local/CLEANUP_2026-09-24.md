# Repository cleanup — 2026-09-24

Scope: `master` in `D:\GitHub\llama.cpp-with-GUI`, continuing the pass recorded
in `REPOSITORY_AUDIT_2026-08-28.md`. That audit's follow-up item 2 asked to
apply the benchmark retention policy to old ignored raw artifacts under
`build_logs/`; this pass does that and sweeps the local state around it.

## Committed

- `chore(gitignore)` (`6d347f348`): dropped the dead GUI 1.0 rules
  (`gui/__pycache__/`, `gui/*.pyc` — `gui/` no longer exists) and named the
  local-only working copies explicitly. `.venv-gui` and `.pytest_cache` were
  only invisible because both write their own `*` ignore file, so a fresh
  checkout could have tracked either of them.

## Removed

| What | Where | Size |
| --- | --- | --- |
| Aborted Hugging Face downloads | `models/.cache/huggingface/download/*.incomplete` | 17.05 GiB |
| BF16 model source of the unmerged Q4 work | `subProject_q4/model_source/Qwen3.6-27B-BF16-0000{1,2}-of-00002.gguf` | 50.11 GiB |
| Stray VQ payload in a log archive | `build_logs/archive/agent-workload-legacy-2026-08/vq-pre-full.bin` | 13.28 GiB |
| Raw ISA fat dumps | `docs/research/rdna4-architecture/isa/*.fat` | 277 MiB |
| Unreferenced `*.server.log` | `build_logs/archive`, `build_logs/swe-overnight` | 150 MiB |
| Root build logs and caches | `build-*full-build.log`, `build-*fix-build.log`, `build-rocm-full-configure.log`, `build_logs/g00-rocm71-rebuild.log`, `__pycache__/`, empty `benches/`, stray `NUL` | ~106 MiB |

The four `.fat` dumps were containers; the code objects extracted from them
(`ggml-hip.gfx1201.tu*.co`, 539 files) and every disassembly remain, so no
research evidence was lost. The two `.incomplete` downloads were last touched
on 2025-12-22 and no download process was running.

The log sweep kept every file whose name appears in a tracked document: 109 of
the 1187 `*.server.log` files in the two swept folders survive, including the
`d094-*`, `d095-*` and `d096-*` server logs quoted by `RESULTS_LOG.md`,
`EXPERIMENTS_DIGEST.md` and the `major-topology/` notes.
`build_logs/agent-workload/BENCH_RUNS.csv` (303 rows) and the rest of the
canonical generated history are untouched, and `git status` stayed clean
throughout — nothing tracked was removed.

`build_logs` fell from 13.8 GiB to 432 MiB; 63.7 GiB of free space came back.

## Deliberately kept

- `docs/research/rdna4-architecture/isa/` — the `.co` objects and
  `.disasm.txt` files (482 MiB) are the evidence W12 and W20 quote, and they
  cannot be re-extracted once the builds that produced them are gone.
- `models/fp4-dl/Qwen3.8-27B-BF16-00001-of-00002.gguf` (3.0 GiB) — named by
  `W20_MXFP4_DECODE_MEASURED.md`.
- `bench2-bins/` (419 MiB) — staged stock and fork binaries A/B runs depend
  on; named by `D136_STOCK_VULKAN_DECODE_EDGE.md` and the stock-binary notes.
- `build-vulkan-linux/`, `build-rocm-linux/` (1.0 GiB) — the Linux lane that
  `LINUX_FP8_RESULTS.md`, `GUI2.md` and `scripts/bench2.py` name, and the
  backend-hint fixtures in `gui2/tests/test_memory.py`.
- `tmp/` (65 MiB) — the reversible root-cleanup stash from the August audit.
- `LINUX_FP8_RESULTS.md`, `ROCm10_BACKEND_CANDIDATES.md`,
  `WINDOWS_FP8_REFERENCE.md` — kept in the root by decision of 2026-09-24 even
  though no tracked file links them.

## Follow-up candidates

1. `build_logs/rpc-package-3080` (67 MiB) is a packaged `rpc-server.exe` with
   its MinGW runtime DLLs from 2026-08-22, unreferenced by any tracked file
   other than this note. Kept because a portable worker bundle costs a repack,
   not just a rebuild.
2. `build_logs/archive/agent-workload-legacy-2026-08` still holds 3438
   `.bin`, `.md`, `.csv` and `.jsonl` artifacts (11.6 MiB) from the legacy
   workload. They are outside the canonical history and could follow the same
   rule once the Q4/agent-workload notes stop needing them.
3. 33 of 153 tracked scripts have no inbound reference. 23 are upstream files
   that an upstream sync would restore anyway, and the fork-only ones are all
   under 14 KiB, so removal was judged not worth the merge cost.
